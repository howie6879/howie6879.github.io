<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>howie6879</title>
    <link>https://www.howie6879.cn/index.xml</link>
    <description>Recent content on howie6879</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-us</language>
    <lastBuildDate>Thu, 24 May 2018 11:40:18 +0800</lastBuildDate>
    <atom:link href="https://www.howie6879.cn/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>博客升级HTTPS方案</title>
      <link>https://www.howie6879.cn/post/36/</link>
      <pubDate>Thu, 24 May 2018 11:40:18 +0800</pubDate>
      
      <guid>https://www.howie6879.cn/post/36/</guid>
      <description>&lt;p&gt;很早就想将博客升级为HTTPS，在心中也确立了方案，步骤也很简单：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;博文上传至Git&lt;/li&gt;
&lt;li&gt;Caddy结合Webhook自动更新，顺便上HTTPS&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;流程很简单，麻烦在于需要Caddy以及Webhook的设置，然后一台服务器也是必须的，这里本人推荐使用&lt;a href=&#34;https://bwh1.net/cart.php?a=confproduct&amp;amp;i=0&#34;&gt;搬瓦工的年付19.9$&lt;/a&gt;方案，购买之后，选择CentOS7，安装后进入shell：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;yum install git vim wget
# 创建用户
useradd howie
# 设定密码
passwd username
# 添加 sudo 权限
visudo
# 找到 
root  ALL=(ALL)   ALL 
# 添加 
howie  ALL=(ALL)   ALL
# 用刚创建的用户名登录
# 安装Caddy以及插件，这里我列出了我需要的插件 各位酌情选择
curl https://getcaddy.com | bash -s personal http.git,http.realip,http.ratelimit,http.ipfilter
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;首先是从git拉取代码，因为我们还是希望服务器可以自动从git远程拉取代码并且自动更新：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;mkdir git
cd git
git clone https://github.com/howie6879/howie6879.github.io.git
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;然后是编写&lt;code&gt;Caddyfile&lt;/code&gt;，这是&lt;code&gt;Caddy&lt;/code&gt;的配置文件，我将配置文件写在&lt;code&gt;/etc/caddy/Caddyfile&lt;/code&gt;，贴上我的配置内容：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;cd ~
mkdir /etc/caddy/
vim /etc/caddy/Caddyfile
# 输入
www.howie6879.cn {
    root /home/howie/git/howie6879.github.io
    tls xiaozizayang@gmail.com
    timeouts none
    gzip
}
howie6879.cn {
    redir www.howie6879.cn
}

# 这里需要注意，请先将ip和博客域名解析好
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Caddy官方也提供了实现脚本，见&lt;a href=&#34;https://github.com/mholt/caddy/blob/master/dist/init/linux-systemd/caddy.service&#34;&gt;这里&lt;/a&gt;，不过我就按照自己的习惯部署了，没有参考官方的&lt;/p&gt;

&lt;p&gt;接下来就可以直接启动我们的博客服务了，Caddy会自动申请证书上HTTPS，非常方便，启动：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;caddy -conf /etc/caddy/Caddyfile
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;访问：&lt;a href=&#34;https://www.howie6879.cn/&#34;&gt;https://www.howie6879.cn/&lt;/a&gt; ，一切正常，至此，博客算迁移完成了，现在还有一个麻烦的地方在于，每次提交更新到Git仓库，总是需要手动下拉一次代码来更新网站博文，这样感觉比较麻烦&lt;/p&gt;

&lt;p&gt;或许可以让Caddy每隔多长时间自动拉取最新的仓库内容，比如60S，这样配置：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;www.howie6879.cn {
    root /home/howie/git/howie6879.github.io
    tls xiaozizayang@gmail.com
    git {
        repo     https://github.com/howie6879/howie6879.github.io.git
        path     /var/www/howie6879.github.io
        interval 60
    }
    timeouts none
    gzip
}
howie6879.cn {
    redir www.howie6879.cn
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;但这样有点浪费资源，还是做成被动触发比较好，所以还是让我们的Webhook登场吧，我暂时有点懒，懒得动。。。&lt;/p&gt;

&lt;p&gt;最后说下，利用&lt;code&gt;Supervisor&lt;/code&gt;管理&lt;code&gt;Caddy&lt;/code&gt;的事情，毕竟上面的启动方式还是不行的:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;sudo yum install python-setuptools
sudo easy_install supervisor

sudo mkdir /etc/supervisord.d/
sudo echo_supervisord_conf &amp;gt; /etc/supervisord.conf

sudo chmod -R 777 /etc/supervisord.d/
sudo chmod -R 777 /etc/caddy/Caddyfile
sudo chmod -R 777 /etc/supervisord.conf

vim /etc/supervisord.con

mkdir ~/supervisor/
# 加上这行
[include]
files = /etc/supervisord.d/*.ini
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;在&lt;code&gt;/etc/supervisord.d/&lt;/code&gt;增加&lt;code&gt;caddy.ini&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;[program:caddy]
command      = caddy -conf=“/etc/caddy/Caddyfile”
user         = howie
process_name = %(program_name)s
autostart    = true
autorestart  = true
startsecs    = 3
redirect_stderr         = true
stdout_logfile_maxbytes = 500MB
stdout_logfile_backups  = 10
stdout_logfile          = ~/supervisor/caddy.log
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;至此，大功告成&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Sanic 使用教程 - 6.常用的技巧</title>
      <link>https://www.howie6879.cn/post/35/</link>
      <pubDate>Sat, 13 Jan 2018 19:12:30 +0800</pubDate>
      
      <guid>https://www.howie6879.cn/post/35/</guid>
      <description>

&lt;p&gt;结合前面讲的配置、项目结构、页面渲染、数据库连接，构造一个优雅的Sanic应用对你来说估计没什么大问题了，但是在实际使用过程中，可能你会碰到各种各样的需求，与之对应，你也会遇到千奇百怪的问题，除了在官方pro提issue，你大部分问题都需要自己去面对，看官方的介绍大概就可以明白&lt;code&gt;Sanic&lt;/code&gt;框架的重心不会放在诸如&lt;code&gt;session cache reload authorized&lt;/code&gt;这些问题上&lt;/p&gt;

&lt;p&gt;本文最新内容可到github上阅读 &lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer/blob/master/docs/part1/6.%E5%B8%B8%E7%94%A8%E7%9A%84%E6%8A%80%E5%B7%A7.md&#34;&gt;6.常用的技巧&lt;/a&gt;&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;Async Python 3.5+ web server that&amp;rsquo;s written to go fast&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;此篇我将将我遇到的一些问题以及解决方案一一记录下来，估计会持续更新，因为问题是不断的哈哈，可能有些问题与前面讲的有些重复，你大可略过，我将其总结成一些小技巧，供大家参考，具体如下：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;api请求json参数以及api接口验证&lt;/li&gt;
&lt;li&gt;gRPC的异步调用方式&lt;/li&gt;
&lt;li&gt;Blueprint&lt;/li&gt;
&lt;li&gt;html&amp;amp;templates编写&lt;/li&gt;
&lt;li&gt;cache&lt;/li&gt;
&lt;li&gt;热加载&lt;/li&gt;
&lt;li&gt;session&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;对于一些问题，我将编写一个小服务来演示这些技巧，具体见&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer/tree/master/examples/demo06&#34;&gt;demo06&lt;/a&gt;，依旧使用前面rss的那个例子，经过修改一番后的rss例子现在目录变成这样子了，里面加了我遇到的各种问题的解决方案，或许你只需要关注你想要了解的就好，如果你有其他的问题，欢迎issue提问，目录大概如下所示：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;src
├── config
│   ├── __init__.py
│   ├── config.py
│   ├── dev_config.py
│   └── pro_config.py
├── database
│   ├── __init__.py
│   └── redis_base
├── grpc_service
│   ├── __init__.py
│   ├── grpc_asyncio_client.py
│   ├── grpc_client.py
│   ├── grpc_server.py
│   ├── hello_grpc.py
│   ├── hello_pb2.py
│   ├── hello_pb2_grpc.py
│   └── proto
├── statics
│   ├── rss_html
│   │   ├── css
│   │   └── js
│   └── rss_json
│       ├── css
│       └── js
├── templates
│   ├── rss_html
│   └── rss_json
├── tools
│   ├── __init__.py
│   └── mid_decorator.py
├── views
│   ├── __init__.py
│   ├── rss_api.py
│   ├── rss_html.py
│   └── rss_json.py
└── run.py
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;和前面相比，可以看到目录里面增加了几个目录和文件，一个是关于数据库的&lt;code&gt;database&lt;/code&gt;，以及关于gRPC的&lt;code&gt;grpc_service&lt;/code&gt;，其实主要是为了演示而已，如果想了解，就可以看关于gRPC的部分，不喜欢就看其他的。&lt;/p&gt;

&lt;p&gt;增加的文件是&lt;code&gt;rss_json.py&lt;/code&gt;，假设这是个接口文件吧，将会根据你的请求参数返回一串json，具体还是建议直接去看代码吧，点这里&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer/tree/master/examples/demo06/sample&#34;&gt;sample&lt;/a&gt;。&lt;/p&gt;

&lt;h3 id=&#34;验证问题&#34;&gt;验证问题&lt;/h3&gt;

&lt;p&gt;假设你正在编写一个api服务，比如根据传的blog名字返回其rss数据，比如&lt;code&gt;rss_json.py&lt;/code&gt;：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;#!/usr/bin/env python
from feedparser import parse
from sanic import Blueprint
from sanic.response import json

api_bp = Blueprint(&#39;rss_api&#39;, url_prefix=&#39;v1&#39;)


@api_bp.route(&amp;quot;/get/rss/&amp;lt;param&amp;gt;&amp;quot;)
async def get_rss_json(request, param):
    if param == &#39;howie6879&#39;:
        url = &amp;quot;http://blog.howie6879.cn/atom.xml&amp;quot;
        feed = parse(url)
        articles = feed[&#39;entries&#39;]
        data = []
        for article in articles:
            data.append({&amp;quot;title&amp;quot;: article[&amp;quot;title_detail&amp;quot;][&amp;quot;value&amp;quot;], &amp;quot;link&amp;quot;: article[&amp;quot;link&amp;quot;]})
        return json(data)
    else:
        return json({&#39;info&#39;: &#39;请访问 http://0.0.0.0:8000/v1/get/rss/howie6879&#39;})
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;启动服务后，此时用&lt;code&gt;GET&lt;/code&gt;方式访问&lt;code&gt;http://0.0.0.0:8000/v1/get/rss/howie6879&lt;/code&gt;，就会返回一串你需要的json，这样使用，没什么问题，好，下面改成post请求，其中请求的参数如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-json&#34;&gt;{
    &amp;quot;name&amp;quot;: &amp;quot;howie6879&amp;quot;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;在&lt;code&gt;rss_json.py&lt;/code&gt;中加入一个新的视图函数：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;@api_bp.route(&amp;quot;/post/rss/&amp;quot;, methods=[&#39;POST&#39;])
async def post_rss_json(request, **kwargs):
    post_data = json_loads(str(request.body, encoding=&#39;utf-8&#39;))
    name = post_data.get(&#39;name&#39;)
    if name == &#39;howie6879&#39;:
        url = &amp;quot;http://blog.howie6879.cn/atom.xml&amp;quot;
        feed = parse(url)
        articles = feed[&#39;entries&#39;]
        data = []
        for article in articles:
            data.append({&amp;quot;title&amp;quot;: article[&amp;quot;title_detail&amp;quot;][&amp;quot;value&amp;quot;], &amp;quot;link&amp;quot;: article[&amp;quot;link&amp;quot;]})
        return json(data)
    else:
        return json({&#39;info&#39;: &#39;参数错误&#39;})
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;发送post请求到&lt;code&gt;http://0.0.0.0:8000/v1/post/rss/&lt;/code&gt;，依旧和上面的结果一样，代码里面有个问题，我们需要判断&lt;code&gt;name&lt;/code&gt;参数是不是存在于post过来的data中，解决方案很简单，加一个判断就好了，可这个是最佳方案么？&lt;/p&gt;

&lt;p&gt;显然并不是的，如果有十个参数呢？然后再增加十几个路由呢？每个路由里有十个参数需要判断，想象一下，这样实施下来，难道要在代码里面堆积满屏幕的判断语句么？这样实在是太可怕了，我们需要更好更通用的解决方案，是时候写个装饰器来验证参数了，打开&lt;code&gt;mid_decorator.py&lt;/code&gt;文件，添加一个验证的装饰器函数：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;def auth_params(*keys):
    &amp;quot;&amp;quot;&amp;quot;
    api请求参数验证
    :param keys: params
    :return:
    &amp;quot;&amp;quot;&amp;quot;

    def wrapper(func):
        @wraps(func)
        async def auth_param(request=None, rpc_data=None, *args, **kwargs):
            request_params, params = {}, []
            if isinstance(request, Request):
                # sanic request
                if request.method == &#39;POST&#39;:
                    try:
                        post_data = json_loads(str(request.body, encoding=&#39;utf-8&#39;))
                    except Exception as e:
                        return response_handle(request, {&#39;info&#39;: &#39;error&#39;})
                    else:
                        request_params.update(post_data)
                        params = [key for key, value in post_data.items() if value]
                elif request.method == &#39;GET&#39;:
                    request_params.update(request.args)
                    params = [key for key, value in request.args.items() if value]
                else:
                    return response_handle(request, {&#39;info&#39;: &#39;error&#39;})
            else:
                pass

            if set(keys).issubset(set(params)):
                kwargs[&#39;request_params&#39;] = request_params
                return await dec_func(func, request, *args, **kwargs)
            else:
                return response_handle(request, {&#39;info&#39;: &#39;error&#39;})

        return auth_param

    return wrapper


async def dec_func(func, request, *args, **kwargs):
    try:
        response = await func(request, *args, **kwargs)
        return response
    except Exception as e:
        return response_handle(request, {&#39;info&#39;: &#39;error&#39;})
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;注意，上面增加的路由函数改为这样：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;@api_bp.route(&amp;quot;/post/rss/&amp;quot;, methods=[&#39;POST&#39;])
@auth_params(&#39;name&#39;)
async def post_rss_json(request, **kwargs):
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;这样么一个请求进来，就会验证name是否存在，而在视图函数里面，就可以放心大胆地使用传进来的参数了，而且对于其他不同的参数验证，只要按照这个写法，直接增加验证参数就好，十分灵活方便。&lt;/p&gt;

&lt;p&gt;对于请求验证的问题，解决方法也是类似，就是利用装饰器来实现，我自己也实现过，在上面的代码链接里面可以找到，不过现在的&lt;code&gt;Sanic&lt;/code&gt;官方已经提供了&lt;code&gt;demo&lt;/code&gt;，见&lt;a href=&#34;https://github.com/channelcat/sanic/blob/master/examples/authorized_sanic.py&#34;&gt;这里&lt;/a&gt;&lt;/p&gt;

&lt;h3 id=&#34;grpc的异步调用方式&#34;&gt;gRPC的异步调用方式&lt;/h3&gt;

&lt;p&gt;在编写微服务的时候，除了需要支持&lt;code&gt;http&lt;/code&gt;请求外，一般还需要支持&lt;code&gt;gRPC&lt;/code&gt;请求，我在使用&lt;code&gt;Sanic&lt;/code&gt;编写微服务的时候，遇到关于异步请求&lt;code&gt;RPC&lt;/code&gt;的需求，当时确实困扰了我，意外发现了这个库&lt;a href=&#34;https://github.com/vmagamedov/grpclib&#34;&gt;grpclib&lt;/a&gt;，进入&lt;code&gt;src/grpc_service&lt;/code&gt;目录，里面就是解决方案，很简单，这里就不多说了，直接看代码就好。&lt;/p&gt;

&lt;h3 id=&#34;blueprint&#34;&gt;Blueprint&lt;/h3&gt;

&lt;p&gt;Blueprint前面的章节有仔细聊过，这里不多说，借用官方文档的例子，一个简单的sanic服务就搭好了：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# main.py
from sanic import Sanic
from sanic.response import json

app = Sanic()

@app.route(&amp;quot;/&amp;quot;)
async def test(request):
    return json({&amp;quot;hello&amp;quot;: &amp;quot;world&amp;quot;})

#访问http://0.0.0.0:8000/即可
if __name__ == &amp;quot;__main__&amp;quot;:
    app.run(host=&amp;quot;0.0.0.0&amp;quot;, port=8000)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;上面的例子可以当做一个完整的小应用，关于Blueprint的概念，可以这么理解，一个蓝图可以独立完成某一个任务，包括模板文件，静态文件，路由都是独立的，而一个应用可以通过注册许多蓝图来进行构建。
比如我现在编写的项目，我使用的是功能式架构，具体如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;├── server.py
├── static
│   └── novels
│       ├── css
│       │   └── result.css
│       ├── img
│       │   └── read_content.png
│       └── js
│           └── main.js
├── template
│   └── novels
│       └── index.html
└── views
    └── novels_blueprint.py
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;可以看到，总的templates以及静态文件还是放在一起，但是不同的blueprint则放在对应的文件夹中，还有一种分区式架构，则是将不同的templats以及static等文件夹全都放在不同的的blueprint中。
最后只要将每个单独的blueprint在主启动文件进行注册就好，上面的目录树是我以前的一个项目&lt;a href=&#34;https://github.com/howie6879/owllook&#34;&gt;owllook&lt;/a&gt;的，也是用&lt;code&gt;Sanic&lt;/code&gt;编写的，有兴趣可以看看，不过现在结构改变挺大。&lt;/p&gt;

&lt;h3 id=&#34;html-templates编写&#34;&gt;html&amp;amp;templates编写&lt;/h3&gt;

&lt;p&gt;编写web服务，自然会涉及到html，sanic自带有html函数，但这并不能满足有些需求，故引入jinja2迫在眉睫，使用方法也很简单：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# 适用python3.5+
# 代码片段，明白意思就好
from sanic import Blueprint
from jinja2 import Environment, PackageLoader, select_autoescape

# 初始化blueprint并定义静态文件夹路径
bp = Blueprint(&#39;novels_blueprint&#39;)
bp.static(&#39;/static&#39;, &#39;./static/novels&#39;)

# jinjia2 config
env = Environment(
    loader=PackageLoader(&#39;views.novels_blueprint&#39;, &#39;../templates/novels&#39;),
    autoescape=select_autoescape([&#39;html&#39;, &#39;xml&#39;, &#39;tpl&#39;]))

def template(tpl, **kwargs):
    template = env.get_template(tpl)
    return html(template.render(kwargs))
    
@bp.route(&amp;quot;/&amp;quot;)
async def index(request):
    return template(&#39;index.html&#39;, title=&#39;index&#39;)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果是&lt;code&gt;python3.6&lt;/code&gt;，可以试试下面的写法：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# 适用python3.5+
# 代码片段，明白意思就好
#!/usr/bin/env python
import sys

from feedparser import parse
from jinja2 import Environment, PackageLoader, select_autoescape
from sanic import Blueprint
from sanic.response import html

from src.config import CONFIG

# https://github.com/channelcat/sanic/blob/5bb640ca1706a42a012109dc3d811925d7453217/examples/jinja_example/jinja_example.py
# 开启异步特性  要求3.6+
enable_async = sys.version_info &amp;gt;= (3, 6)

html_bp = Blueprint(&#39;rss_html&#39;, url_prefix=&#39;html&#39;)
html_bp.static(&#39;/statics/rss_html&#39;, CONFIG.BASE_DIR + &#39;/statics/rss_html&#39;)

# jinjia2 config
env = Environment(
    loader=PackageLoader(&#39;views.rss_html&#39;, &#39;../templates/rss_html&#39;),
    autoescape=select_autoescape([&#39;html&#39;, &#39;xml&#39;, &#39;tpl&#39;]),
    enable_async=enable_async)


async def template(tpl, **kwargs):
    template = env.get_template(tpl)
    rendered_template = await template.render_async(**kwargs)
    return html(rendered_template)
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;cache&#34;&gt;cache&lt;/h3&gt;

&lt;p&gt;我在项目中主要使用redis作为缓存，使用&lt;a href=&#34;https://github.com/argaen/aiocache&#34;&gt;aiocache&lt;/a&gt;很方便就完成了我需要的功能，当然自己利用aioredis编写也不会复杂到哪里去。&lt;/p&gt;

&lt;p&gt;比如上面的例子，每次访问&lt;code&gt;http://0.0.0.0:8000/v1/get/rss/howie6879&lt;/code&gt;，都要请求一次对应的rss资源，如果做个缓存那岂不是简单很多？&lt;/p&gt;

&lt;p&gt;改写成这样：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;@cached(ttl=1000, cache=RedisCache, key=&amp;quot;rss&amp;quot;, serializer=PickleSerializer(), port=6379, namespace=&amp;quot;main&amp;quot;)
async def get_rss():
    print(&amp;quot;第一次请求休眠3秒...&amp;quot;)
    await asyncio.sleep(3)
    url = &amp;quot;http://blog.howie6879.cn/atom.xml&amp;quot;
    feed = parse(url)
    articles = feed[&#39;entries&#39;]
    data = []
    for article in articles:
        data.append({&amp;quot;title&amp;quot;: article[&amp;quot;title_detail&amp;quot;][&amp;quot;value&amp;quot;], &amp;quot;link&amp;quot;: article[&amp;quot;link&amp;quot;]})
    return data


@api_bp.route(&amp;quot;/get/rss/&amp;lt;name&amp;gt;&amp;quot;)
async def get_rss_json(request, name):
    if name == &#39;howie6879&#39;:
        data = await get_rss()
        return json(data)
    else:
        return json({&#39;info&#39;: &#39;请访问 http://0.0.0.0:8000/v1/get/rss/howie6879&#39;})
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;为了体现缓存的速度，首次请求休眠3秒，请求过后，redis中就会将此次json数据缓存进去了，下次去请求就会直接冲redis读取数据。&lt;/p&gt;

&lt;p&gt;带上装饰器，什么都解决了。&lt;/p&gt;

&lt;h3 id=&#34;热加载&#34;&gt;热加载&lt;/h3&gt;

&lt;p&gt;我发现有个pr实现了这个需求，如果你有兴趣，可以看&lt;a href=&#34;https://github.com/channelcat/sanic/pull/1047&#34;&gt;这里&lt;/a&gt;&lt;/p&gt;

&lt;h3 id=&#34;session&#34;&gt;session&lt;/h3&gt;

&lt;p&gt;sanic对此有一个第三方插件&lt;a href=&#34;https://github.com/subyraman/sanic_session&#34;&gt;&lt;code&gt;sanic_session&lt;/code&gt;&lt;/a&gt;，用法非常简单，有兴趣可以看看&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Sanic 使用教程 - 5.数据库使用</title>
      <link>https://www.howie6879.cn/post/34/</link>
      <pubDate>Fri, 12 Jan 2018 10:50:30 +0800</pubDate>
      
      <guid>https://www.howie6879.cn/post/34/</guid>
      <description>

&lt;p&gt;介绍中说的很明白，&lt;code&gt;Sanic&lt;/code&gt; 是一个可以使用 &lt;code&gt;async/await&lt;/code&gt; 语法编写项目的&lt;strong&gt;异步&lt;/strong&gt;非阻塞框架，既然是异步框架，那么在使用过程中用到的第三方包也最好是异步的，比如http请求，最好就使用&lt;code&gt;aihttp&lt;/code&gt;而非&lt;code&gt;requests&lt;/code&gt;，对于数据库的连接，也是同样如此，下面我将用代码的形式来说明下如何在Sanic中连接数据库。&lt;/p&gt;

&lt;h3 id=&#34;操作mysql&#34;&gt;操作Mysql&lt;/h3&gt;

&lt;p&gt;对于mysql数据库的异步操作，我只在一些脚本中用过，用的是&lt;a href=&#34;https://github.com/aio-libs/aiomysql&#34;&gt;aiomysql&lt;/a&gt;，其中官方文档中讲得很清楚，也支持结合&lt;code&gt;sqlalchemy&lt;/code&gt;编写&lt;code&gt;ORM&lt;/code&gt;，然后aiomysql提供了自己编写的异步引擎。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;from aiomysql.sa import create_engine
# 这个才是关键
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;下面我编写一个具体的例子来用异步语句操作下数据库，首先建立如下目录：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shel&#34;&gt;aio_mysql
├── demo.py
├── model.py
└── requirements.txt
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;建立表：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-mysql&#34;&gt;create database test_mysql;

CREATE TABLE user
(
  id        INT AUTO_INCREMENT
    PRIMARY KEY,
  user_name VARCHAR(16) NOT NULL,
  pwd       VARCHAR(32) NOT NULL,
  real_name VARCHAR(6)  NOT NULL
);
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;一切准备就绪，下面编写代码：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# script: model.py
import sqlalchemy as sa

metadata = sa.MetaData()

user = sa.Table(
    &#39;user&#39;,
    metadata,
    sa.Column(&#39;id&#39;, sa.Integer, autoincrement=True, primary_key=True),
    sa.Column(&#39;user_name&#39;, sa.String(16), nullable=False),
    sa.Column(&#39;pwd&#39;, sa.String(32), nullable=False),
    sa.Column(&#39;real_name&#39;, sa.String(6), nullable=False),
)

# script: demo.py
import asyncio

from aiomysql.sa import create_engine

from model import user,metadata


async def go(loop):
    &amp;quot;&amp;quot;&amp;quot;
    aiomysql项目地址：https://github.com/aio-libs/aiomysql
    :param loop:
    :return:
    &amp;quot;&amp;quot;&amp;quot;
    engine = await create_engine(user=&#39;root&#39;, db=&#39;test_mysql&#39;,
                                 host=&#39;127.0.0.1&#39;, password=&#39;123456&#39;, loop=loop)
    async with engine.acquire() as conn:
        await conn.execute(user.insert().values(user_name=&#39;user_name01&#39;, pwd=&#39;123456&#39;, real_name=&#39;real_name01&#39;))
        await conn.execute(&#39;commit&#39;)

        async for row in conn.execute(user.select()):
            print(row.user_name, row.pwd)

    engine.close()
    await engine.wait_closed()


loop = asyncio.get_event_loop()
loop.run_until_complete(go(loop))

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;运行 python demo.py，会看到如下输出：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;user_name01 123456
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;很简单吧，具体示例见&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer/tree/master/examples/demo05/aio_mysql&#34;&gt;aio_mysql&lt;/a&gt;&lt;/p&gt;

&lt;h3 id=&#34;操作mongodb&#34;&gt;操作MongoDB&lt;/h3&gt;

&lt;p&gt;我业余写的一个项目，基本用的就是&lt;code&gt;MongoDB&lt;/code&gt;来储存数据，对于异步操作&lt;code&gt;MongoDB&lt;/code&gt;，目前Python主要用的是&lt;a href=&#34;https://motor.readthedocs.io/en/stable/index.html&#34;&gt;motor&lt;/a&gt;，使用起来依旧很简单，但是结合具体功能，就有不同的的需求，最后就会形成各种各样的连接方案，这里我主要分享下自己是如何使用的，目录如下所示：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;aio_mongo
├── demo.py
└── requirements.txt
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;MongoDB&lt;/code&gt;是一个基于分布式文件存储的数据库，它介于关系数据库和非关系数据库之间，所以它使用起来也是比较灵活的，打开&lt;code&gt;demo.py&lt;/code&gt;：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;#!/usr/bin/env python
import os

from functools import wraps

from motor.motor_asyncio import AsyncIOMotorClient

MONGODB = dict(
    MONGO_HOST=os.getenv(&#39;MONGO_HOST&#39;, &amp;quot;&amp;quot;),
    MONGO_PORT=os.getenv(&#39;MONGO_PORT&#39;, 27017),
    MONGO_USERNAME=os.getenv(&#39;MONGO_USERNAME&#39;, &amp;quot;&amp;quot;),
    MONGO_PASSWORD=os.getenv(&#39;MONGO_PASSWORD&#39;, &amp;quot;&amp;quot;),
    DATABASE=&#39;test_mongodb&#39;,
)

class MotorBaseOld:
    &amp;quot;&amp;quot;&amp;quot;
    默认实现了一个db只创建一次，缺点是更换集合麻烦
    &amp;quot;&amp;quot;&amp;quot;
    _db = None
    MONGODB = MONGODB

    def client(self, db):
        # motor
        self.motor_uri = &#39;mongodb://{account}{host}:{port}/{database}&#39;.format(
            account=&#39;{username}:{password}@&#39;.format(
                username=self.MONGODB[&#39;MONGO_USERNAME&#39;],
                password=self.MONGODB[&#39;MONGO_PASSWORD&#39;]) if self.MONGODB[&#39;MONGO_USERNAME&#39;] else &#39;&#39;,
            host=self.MONGODB[&#39;MONGO_HOST&#39;] if self.MONGODB[&#39;MONGO_HOST&#39;] else &#39;localhost&#39;,
            port=self.MONGODB[&#39;MONGO_PORT&#39;] if self.MONGODB[&#39;MONGO_PORT&#39;] else 27017,
            database=db)
        return AsyncIOMotorClient(self.motor_uri)

    @property
    def db(self):
        if self._db is None:
            self._db = self.client(self.MONGODB[&#39;DATABASE&#39;])[self.MONGODB[&#39;DATABASE&#39;]]

        return self._db
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;我最开始，使用的是这样种方式来连接&lt;code&gt;MongoDB&lt;/code&gt;，上面代码保证了集合中的db被_db维护，保证只会创建一次，如果你项目中不会随意更改集合的话，也没什么大问题，如果不是，我推荐使用下面这样的连接方式，可以自由地更换集合与db：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;
def singleton(cls):
    &amp;quot;&amp;quot;&amp;quot;
    用装饰器实现的实例 不明白装饰器可见附录 装饰器：https://github.com/howie6879/Sanic-For-Pythoneer/blob/master/docs/part2/%E9%99%84%E5%BD%95%EF%BC%9A%E5%85%B3%E4%BA%8E%E8%A3%85%E9%A5%B0%E5%99%A8.md
    :param cls: cls
    :return: instance
    &amp;quot;&amp;quot;&amp;quot;
    _instances = {}

    @wraps(cls)
    def instance(*args, **kw):
        if cls not in _instances:
            _instances[cls] = cls(*args, **kw)
        return _instances[cls]

    return instance

@singleton
class MotorBase:
    &amp;quot;&amp;quot;&amp;quot;
    更改mongodb连接方式 单例模式下支持多库操作
    About motor&#39;s doc: https://github.com/mongodb/motor
    &amp;quot;&amp;quot;&amp;quot;
    _db = {}
    _collection = {}
    MONGODB = MONGODB

    def __init__(self):
        self.motor_uri = &#39;&#39;

    def client(self, db):
        # motor
        self.motor_uri = &#39;mongodb://{account}{host}:{port}/{database}&#39;.format(
            account=&#39;{username}:{password}@&#39;.format(
                username=self.MONGODB[&#39;MONGO_USERNAME&#39;],
                password=self.MONGODB[&#39;MONGO_PASSWORD&#39;]) if self.MONGODB[&#39;MONGO_USERNAME&#39;] else &#39;&#39;,
            host=self.MONGODB[&#39;MONGO_HOST&#39;] if self.MONGODB[&#39;MONGO_HOST&#39;] else &#39;localhost&#39;,
            port=self.MONGODB[&#39;MONGO_PORT&#39;] if self.MONGODB[&#39;MONGO_PORT&#39;] else 27017,
            database=db)
        return AsyncIOMotorClient(self.motor_uri)

    def get_db(self, db=MONGODB[&#39;DATABASE&#39;]):
        &amp;quot;&amp;quot;&amp;quot;
        获取一个db实例
        :param db: database name
        :return: the motor db instance
        &amp;quot;&amp;quot;&amp;quot;
        if db not in self._db:
            self._db[db] = self.client(db)[db]

        return self._db[db]

    def get_collection(self, db_name, collection):
        &amp;quot;&amp;quot;&amp;quot;
        获取一个集合实例
        :param db_name: database name
        :param collection: collection name
        :return: the motor collection instance
        &amp;quot;&amp;quot;&amp;quot;
        collection_key = db_name + collection
        if collection_key not in self._collection:
            self._collection[collection_key] = self.get_db(db_name)[collection]

        return self._collection[collection_key]

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;为了避免重复创建MotorBase实例，可以实现一个单例模式来保证资源的有效利用，具体代码以及运行demo见&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer/tree/master/examples/demo05/aio_mongo&#34;&gt;aio_mongo&lt;/a&gt;&lt;/p&gt;

&lt;h3 id=&#34;操作redis&#34;&gt;操作Redis&lt;/h3&gt;

&lt;p&gt;对于Redis的异步操作，我选用的是&lt;code&gt;asyncio_redis&lt;/code&gt;，你大可不必非要使用这个，或许其他的库实现地更好，我只是用这个举个例子，建立如下目录：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;aio_redis
├── demo.py
└── requirements.txt
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;建立一个redis连接池：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;#!/usr/bin/env python
import os
import asyncio_redis

REDIS_DICT = dict(
    IS_CACHE=True,
    REDIS_ENDPOINT=os.getenv(&#39;REDIS_ENDPOINT&#39;, &amp;quot;localhost&amp;quot;),
    REDIS_PORT=os.getenv(&#39;REDIS_PORT&#39;, 6379),
    REDIS_PASSWORD=os.getenv(&#39;REDIS_PASSWORD&#39;, None),
    DB=0,
    POOLSIZE=10,
)


class RedisSession:
    &amp;quot;&amp;quot;&amp;quot;
    建立redis连接池
    &amp;quot;&amp;quot;&amp;quot;
    _pool = None

    async def get_redis_pool(self):
        if not self._pool:
            self._pool = await asyncio_redis.Pool.create(
                host=str(REDIS_DICT.get(&#39;REDIS_ENDPOINT&#39;, &amp;quot;localhost&amp;quot;)), port=int(REDIS_DICT.get(&#39;REDIS_PORT&#39;, 6379)),
                poolsize=int(REDIS_DICT.get(&#39;POOLSIZE&#39;, 10)), password=REDIS_DICT.get(&#39;REDIS_PASSWORD&#39;, None),
                db=REDIS_DICT.get(&#39;DB&#39;, None)
            )

        return self._pool

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;具体见&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer/blob/master/examples/demo05/aio_redis/demo.py&#34;&gt;aio_redis&lt;/a&gt;，使用起来很简单，不做多叙述。&lt;/p&gt;

&lt;h3 id=&#34;说明&#34;&gt;说明&lt;/h3&gt;

&lt;p&gt;如果你使用其它类型的数据库，其实使用方式也是类似。
本章代码地址，见&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer/tree/master/examples/demo05&#34;&gt;demo05&lt;/a&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>神经网络与深度学习笔记 - 1.识别手写字</title>
      <link>https://www.howie6879.cn/post/33/</link>
      <pubDate>Mon, 08 Jan 2018 14:57:30 +0800</pubDate>
      
      <guid>https://www.howie6879.cn/post/33/</guid>
      <description>

&lt;p&gt;&lt;img src=&#34;https://www.howie6879.cn/images/post/33/01.jpg&#34; alt=&#34;from Unsplash&#34; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;a href=&#34;http://neuralnetworksanddeeplearning.com/index.html&#34;&gt;Neural Networks and Deep Learning&lt;/a&gt; 这本书主要讲的是如何掌握神经网络的核心概念，包括现代技术的深度学习，为你将来使⽤神经网络和深度学习打下基础，以下是我的读书笔记。&lt;/p&gt;

&lt;p&gt;神经网络是一门重要的机器学习技术，它通过模拟人脑的神经网络来实现人工智能地目的，所以其也是深度学习的基础，了解它之后自然会受益颇多，本章主要是以识别手写字这个问题来贯穿整篇，那么，人类的视觉系统和神经网络到底在识别一个目标的时候，主要区别在哪？&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;人类视觉系统：通过数十亿年不断地进化与学习，最终能够极好地适应理解视觉世界的任务，从而无意识地就可以对目标进行判断识别&lt;/li&gt;
&lt;li&gt;神经网络：通过提供的样本来推断出识别某种目标的规则，作为判断标准&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;本章的主要内容是介绍神经网络的基本概念以及引入一个识别手写数字的例子来加深我们的理解，你将了解到：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;两个重要的人工神经元：感知器和S型神经元&lt;/li&gt;
&lt;li&gt;神经⽹络的架构&lt;/li&gt;
&lt;li&gt;⼀个简单的分类⼿写数字的⽹络&lt;/li&gt;
&lt;li&gt;标准的神经网络学习算法：随机梯度下降算法&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&#34;感知器&#34;&gt;感知器&lt;/h3&gt;

&lt;p&gt;1943年，心理学家McCulloch和数学家Pitts发表了《&lt;a href=&#34;http://cns-pc62.bu.edu/cn550/Readings/mcculloch-pitts-43.pdf&#34;&gt;A logical calculus of the ideas immanent in nervous activity&lt;/a&gt;》，其中提出了抽象的神经元模型MP，但是在这个模型中权重都是要求提前设置好才可以输出目标值，所以很遗憾，它不可以学习，但这不影响此模型给后来者带来的影响，比如感知器：&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;感知器是Frank Rosenblatt提出的一个由两层神经元组成的人工神经网络，它的出现在当时可是引起了轰动，因为感知器是首个可以学习的神经网络&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;感知器的工作方式如下所示：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://www.howie6879.cn/images/post/33/02.jpg&#34; alt=&#34;from nndl&#34; /&gt;&lt;/p&gt;

&lt;p&gt;$x_{1},x_{2},x_{3}$ 分别表示三个不同的二进制输入，output则是一个二进制输出，对于多种输入，可能有的输入成立有的不成立，在这么多输入的影响下，该如何判断输出output呢？Rosenblatt引入了权重。&lt;/p&gt;

&lt;p&gt;对于$x_{1},x_{2},&amp;hellip;,x_{j}$个输入，每个输入都有对应的权重$w_{1},w_{2},&amp;hellip;,w_{j}$，最后的输出output由每个输入与其对应的权重相乘只和与阈值之差$\sum _{j} w{_j}x{_j}$来决定，如下：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://www.howie6879.cn/images/post/33/03.jpg&#34; alt=&#34;from nndl&#34; /&gt;&lt;/p&gt;

&lt;p&gt;假设$b=-threshold$且$w$和$x$对应权重和输⼊的向量，即：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;$x=(x_{1},x_{2},&amp;hellip;,x_{j})$&lt;/li&gt;
&lt;li&gt;$w=(w_{1},w_{2},&amp;hellip;,w_{j})$&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;那么感知器的规则可以重写为:&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://www.howie6879.cn/images/post/33/04.jpg&#34; alt=&#34;from nndl&#34; /&gt;&lt;/p&gt;

&lt;p&gt;这就是感知器的数学模型，是不是就像一个逻辑回归模型？它的出现让我们可以设计学习算法，从而实现自动调整人工神经元的权重和偏置，与此同时output也会随之改变，这就是学习！如果你有兴趣可以看我用&lt;code&gt;python&lt;/code&gt;写的一个感知器自动学习实现与非门，代码在&lt;a href=&#34;https://github.com/howie6879/nndl/blob/master/src/perceptron.py&#34;&gt;这里&lt;/a&gt;。&lt;/p&gt;

&lt;p&gt;说句题外话，由于感知器是单层神经网络，它只能实现简单的线性分类任务，所以它无法对异或问题进行分类，异或的真值表如下：&lt;/p&gt;

&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;$x$&lt;/th&gt;
&lt;th&gt;$y$&lt;/th&gt;
&lt;th&gt;$output$&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;

&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;

&lt;tr&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;/tr&gt;

&lt;tr&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;/tr&gt;

&lt;tr&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;1&lt;/td&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;可以看出来，异或问题是线性不可分的，不信你画个坐标轴试试看，那么问题来了？怎么解决，大部分都能很快地想出解决方案，既然感知器可以实现线性分类，也就是说实现与非门是没有问题的，逻辑上来说我们可以实现任何逻辑功能，但前提是为感知器加入一个隐藏层，意思就是多了一个隐藏层的神经网络之后，就可以解决异或问题。&lt;/p&gt;

&lt;p&gt;可是在当时是没办法对多层神经网络（包括异或逻辑）进行训练的，因为计算量太大了，Minsky在1969年出版了一本叫《Perceptron》的书详细说明了这个问题。&lt;/p&gt;

&lt;p&gt;遇到问题，就解决问题，两层神经网络的作用，是可以对非线性进行分类的，我们的先人并未止步，直到反向传播（Backpropagation，BP）算法的出现解决了计算量太大的问题。&lt;/p&gt;

&lt;p&gt;感知器，终于可以多层了。&lt;/p&gt;

&lt;h3 id=&#34;s型神经元&#34;&gt;S型神经元&lt;/h3&gt;

&lt;p&gt;前面提到，神经网络可以通过样本的学习来调整人工神经元的权重和偏置，从而使输出的结果更加准确，那么怎样给⼀个神经⽹络设计这样的算法呢？&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://www.howie6879.cn/images/post/33/05.jpg&#34; alt=&#34;from nndl&#34; /&gt;&lt;/p&gt;

&lt;p&gt;以数字识别为例，假设⽹络错误地把⼀个&lt;code&gt;9&lt;/code&gt;的图像分类为&lt;code&gt;8&lt;/code&gt;，我们可以让权重和偏置做些⼩的改动，从而达到我们需要的结果&lt;code&gt;9&lt;/code&gt;，这就是学习。对于感知器，我们知道，其返还的结果不是0就是1，很可能出现这样一个情况，我们好不容易将一个目标，比如把&lt;code&gt;9&lt;/code&gt;的图像分类为&lt;code&gt;8&lt;/code&gt;调整回原来正确的分类，可此时的阈值和偏置会造成其他样本的判断失误，这样的调整不是一个好的方案。&lt;/p&gt;

&lt;p&gt;所以，我们需要S型神经元，因为S型神经元返回的是[0,1]之间的任何实数，这样的话权重和偏置的微⼩改动只会引起输出的微⼩变化，此时的output可以表示为$\sigma(w \cdot x+b)$，而$\sigma$就是S型函数，S型函数中S指的是Sigmoid函数，定义如下：&lt;/p&gt;

&lt;p&gt;\begin{equation}
  \sigma(z) \equiv \frac{1}{1+e^{-z}}
  \label{eq:3}
\end{equation}&lt;/p&gt;

&lt;p&gt;这里的$z=\sum _{j} w{_j}x{_j}+b$，那么⼀个具有输⼊$x_{1},x_{2},&amp;hellip;,x_{j}$，权重为$w_{1},w_{2},&amp;hellip;,w_{j}$，和偏置b的S型神经元的输出是：&lt;/p&gt;

&lt;p&gt;\begin{equation}
  \frac{1}{1+\exp(-\sum_j w_j x_j-b)}
  \label{eq:4}
\end{equation}&lt;/p&gt;

&lt;p&gt;上面说过，感知器的激活函数只会输出两个值，分别是&lt;code&gt;0&lt;/code&gt;或&lt;code&gt;1&lt;/code&gt;，让我们把目光投向S型神经元，假设此时$z$是一个很大的正数，那么此时output将无限接近于1，反之，若$z$是一个很大的负数，那么此时output将无限接近于0，从极端情况来看，是不是很像S型神经元呢？&lt;/p&gt;

&lt;p&gt;S型神经元的输出是[0,1]之间的任何实数，那么这个分类模型该如何判断分类结果呢？其实很简单，我们可以定义一个数值，比如：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;$\sigma(z) \leq 0.5$ 输出0&lt;/li&gt;
&lt;li&gt;$\sigma(z)&amp;gt;0.5$  输出1&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&#34;神经-络的架构&#34;&gt;神经⽹络的架构&lt;/h3&gt;

&lt;p&gt;看下面一个四层神经网络，这种类型的多层⽹络有时被称为多层感知器或者&lt;strong&gt;MLP&lt;/strong&gt;，如下图：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://www.howie6879.cn/images/post/33/06.jpg&#34; alt=&#34;from nndl&#34; /&gt;&lt;/p&gt;

&lt;p&gt;需要记住以下几个概念：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;⽹络中最左边的称为&lt;strong&gt;输⼊层&lt;/strong&gt;，其中的神经元称为&lt;strong&gt;输⼊神经元&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;中间得称之为&lt;strong&gt;隐藏层&lt;/strong&gt;，图中有两个隐藏层&lt;/li&gt;
&lt;li&gt;最右边称之为&lt;strong&gt;输出层&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;上⼀层的输出作为下⼀层的输⼊，信息总是向前传播，从不反向回馈：&lt;strong&gt;前馈神经网络&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;有回路，其中反馈环路是可⾏的：&lt;strong&gt;递归神经网络&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&#34;个简单的分类-写数字的-络&#34;&gt;⼀个简单的分类⼿写数字的⽹络&lt;/h3&gt;

&lt;p&gt;对于这六个数字&lt;code&gt;504192&lt;/code&gt;，我们该如何实现一个分类⼿写数字的⽹络呢，可以将其分解为两个小问题：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;504192是连续在一起的图像，首先可以将其分割成6个小图像，比如&lt;code&gt;5&lt;/code&gt; &lt;code&gt;0&lt;/code&gt; &amp;hellip;等&lt;/li&gt;

&lt;li&gt;&lt;p&gt;再对分割开的小图像进行分类，比如识别5

我们将使⽤⼀个三层神经⽹络来识别单个数字：
&lt;img src=&#34;https://www.howie6879.cn/images/post/33/07.jpg&#34; alt=&#34;from nndl&#34; /&gt;
由于训练数据由$28*28$的⼿写数字的图像组成，所以:&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;输入层有784个神经元，因为$784 = 28 \times 28$，输⼊像素是灰度级的，值为0:0表⽰⽩⾊，值为1:0表⽰⿊⾊，中间数值表⽰逐渐暗淡的灰⾊&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;隐藏层⽤n来表⽰神经元的数量，我们将给n实验不同的数值。⽰例中⽤⼀个⼩的隐藏层来说明，仅仅包含$n = 15$个神经元&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;⽹络的输出层包含有10个神经元，如果第一个输出神经元被激活，那么数字就是0，以此类推，从0到9&lt;/p&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;一个分类⼿写数字的⽹络，大概就可以这样实现。&lt;/p&gt;

&lt;h3 id=&#34;随机梯度下降算法&#34;&gt;随机梯度下降算法&lt;/h3&gt;

&lt;p&gt;分类方案已经确定了，接下来第一步就是获取样本，我们将使⽤&lt;a href=&#34;http://yann.lecun.com/exdb/mnist/&#34;&gt;MNIST&lt;/a&gt;数据集，其包含有数以万计的连带着正确分类器的⼿写数字的扫描图像，下载好建立这样的目录结构：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;nndl
├── 01
│   └── mnist_data.py
└── MNIST_data
    ├── t10k-images-idx3-ubyte.gz
    ├── t10k-labels-idx1-ubyte.gz
    ├── train-images-idx3-ubyte.gz
    └── train-labels-idx1-ubyte.gz
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;然后使用&lt;code&gt;TensorFlow&lt;/code&gt;来读取数据，看看下载的数据集到底是什么样子，在&lt;code&gt;/01/mnist_data.py&lt;/code&gt;中添加如下代码：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;#!/usr/bin/env python
&amp;quot;&amp;quot;&amp;quot;
 Created by howie.hu at 10/01/2018.
&amp;quot;&amp;quot;&amp;quot;
from tensorflow.examples.tutorials.mnist.input_data import read_data_sets

mnist = read_data_sets(&amp;quot;../MNIST_data/&amp;quot;, one_hot=True)

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;查看一些基本信息：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;print(&amp;quot;Training data size: %s&amp;quot; % mnist.train.num_examples)
print(&amp;quot;Validating data size: %s&amp;quot; % mnist.validation.num_examples)
print(&amp;quot;Testing data size: %s&amp;quot; % mnist.test.num_examples)

# 每张图片是长度为784的一维数组
print(len(mnist.train.images[0]))

def display_digit(image):
    image = image.reshape([28,28])
    plt.imshow(image, cmap=plt.get_cmap(&#39;gray_r&#39;))
    plt.show()

print(&amp;quot;样本真实数字为：%s&amp;quot; % list(mnist.train.labels[1]).index(1))
# 显示图像
display_digit(mnist.train.images[1])

# Output
# Training data size: 55000
# Validating data size: 5000
# Testing data size: 10000
# 784
# 样本真实数字为：3
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;可以看到显示如下图像：
&lt;img src=&#34;https://www.howie6879.cn/images/post/33/08.jpg&#34; alt=&#34;from mnist&#34; /&gt;&lt;/p&gt;

&lt;p&gt;通过代码，我们知道，每个图像被分割成784个值存于一维数组中，看作⼀个$28 \times 28 = 784$维的向量，每个向量就代表图像中单个像素的灰度值，假设我们用$y$表示output，也就是当我们输入一组样本数据x，那么y就是我们需要的结果，怎么从x得到y呢，我们需要一个函数$y= y(x)$。&lt;/p&gt;

&lt;p&gt;感知器为什么可以学习，因为它可以调整$b$和$w$来实现output的改变，这里，我们同样需要一个算法来让我们找到权重和偏置，从而使得$y= y(x)$可以拟合样本输入的x，为了量化我们如何实现这个⽬标，我们定义⼀个&lt;strong&gt;代价函数&lt;/strong&gt;（有时被称为损失或⽬标函数）：&lt;/p&gt;

&lt;p&gt;\begin{equation}
  C(w,b) \equiv \frac{1}{2n} \sum_x | y(x) - a|^2
  \label{eq:6}
\end{equation}&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;w 表⽰所有的⽹络中权重的集合&lt;/li&gt;
&lt;li&gt;b 是所有的偏置&lt;/li&gt;
&lt;li&gt;n 是训练输⼊数据的个数&lt;/li&gt;
&lt;li&gt;a 是表⽰当输⼊为x时输出的向量，可以理解为output&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;训练模型的过程就是优化代价函数的过程，&lt;em&gt;Cost Function(代价函数)&lt;/em&gt; 越小，就代表模型拟合的越好，所以我们的目的是找出最⼩化权重和偏置的代价函数$C(w,b)$，我们将采⽤称为&lt;strong&gt;梯度下降&lt;/strong&gt;的算法来达到这个⽬的。&lt;/p&gt;

&lt;h3 id=&#34;参考&#34;&gt;参考&lt;/h3&gt;

&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;http://neuralnetworksanddeeplearning.com/index.html&#34;&gt;Neural Networks and Deep Learning&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;http://www.cnblogs.com/subconscious/p/5058741.html&#34;&gt;神经网络浅讲：从神经元到深度学习&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
    </item>
    
    <item>
      <title>Hello World</title>
      <link>https://www.howie6879.cn/about/</link>
      <pubDate>Sat, 06 Jan 2018 00:00:00 +0000</pubDate>
      
      <guid>https://www.howie6879.cn/about/</guid>
      <description>

&lt;h3 id=&#34;介绍&#34;&gt;介绍&lt;/h3&gt;

&lt;p&gt;努力的程序员：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;Github : &lt;a href=&#34;https://github.com/howie6879&#34;&gt;howie6879&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;Email : howie6879@gmail.com&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&#34;开源项目&#34;&gt;开源项目&lt;/h3&gt;

&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/howie6879/owllook&#34;&gt;&lt;strong&gt;owllook&lt;/strong&gt;&lt;/a&gt;: &lt;a href=&#34;https://github.com/howie6879/owllook&#34;&gt;在线网络小说阅读网站&amp;amp;小说搜索引擎&amp;amp;小说推荐系统搜索、追书、收藏、追更、小说API&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/howie6879/hproxy&#34;&gt;&lt;strong&gt;hproxy&lt;/strong&gt;&lt;/a&gt;:  &lt;a href=&#34;https://github.com/howie6879/hproxy&#34;&gt;Asynchronous IP proxy pool, aims to make getting proxy as convenient as possible&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/howie6879/talospider&#34;&gt;&lt;strong&gt;talospider&lt;/strong&gt;&lt;/a&gt;: &lt;a href=&#34;https://github.com/howie6879/talospider&#34;&gt;A simple,lightweight scraping micro-framework&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer&#34;&gt;&lt;strong&gt;Sanic-For-Pythoneer&lt;/strong&gt;&lt;/a&gt;: &lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer&#34;&gt;一份sanic使用教程，开源小书&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&#34;教程&#34;&gt;教程&lt;/h3&gt;

&lt;p&gt;&lt;strong&gt;深度学习&lt;/strong&gt;：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;http://blog.howie6879.cn/tags/%E7%A5%9E%E7%BB%8F%E7%BD%91%E7%BB%9C%E4%B8%8E%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0&#34;&gt;神经网络与深度学习笔记&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;&lt;strong&gt;Sanic&lt;/strong&gt;：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;http://blog.howie6879.cn/tags/sanic&#34;&gt;Sanic-For-Pythoneer&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
    </item>
    
    <item>
      <title>Sanic 使用教程 - 4.展示一个页面</title>
      <link>https://www.howie6879.cn/post/32/</link>
      <pubDate>Fri, 05 Jan 2018 09:20:14 +0000</pubDate>
      
      <guid>https://www.howie6879.cn/post/32/</guid>
      <description>

&lt;p&gt;前面一节介绍&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer/blob/master/docs/part1/3.%E9%A1%B9%E7%9B%AE%E7%BB%93%E6%9E%84.md&#34;&gt;项目结构&lt;/a&gt;的时候，很粗略地讲了下如何将rss的文章内容在网页上进行展示。&lt;/p&gt;

&lt;p&gt;相信你应该已经了解清楚，&lt;code&gt;sanic&lt;/code&gt;是怎么接收请求并返回被请求的资源的，简单来说概括如下：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;接收请求&lt;/li&gt;
&lt;li&gt;找到对应的路由并执行路由对应的视图函数&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;http://jinja.pocoo.org/docs/2.10/&#34;&gt;Jinja2&lt;/a&gt;模板渲染返回视图&lt;/li&gt;
&lt;/ul&gt;

&lt;!--- more ---&gt;

&lt;h3 id=&#34;路由和视图函数&#34;&gt;路由和视图函数&lt;/h3&gt;

&lt;p&gt;在此我假设你理解 &lt;code&gt;python&lt;/code&gt; 中的装饰器，如果你并不清楚，可以看我另写的关于&lt;a href=&#34;http://blog.howie6879.cn/2016/09/28/04/&#34;&gt;装饰器&lt;/a&gt;的介绍，回归正题，还记得第一节中的代码实例么？&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;#!/usr/bin/env python
from sanic import Sanic
from sanic.response import text

app = Sanic()

# 此处将路由 / 与视图函数 test 关联起来
@app.route(&amp;quot;/&amp;quot;)
async def test(request):
    return text(&#39;Hello World!&#39;)


if __name__ == &amp;quot;__main__&amp;quot;:
    app.run(host=&amp;quot;0.0.0.0&amp;quot;, port=8000)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;在前言介绍里，出现这几个名词 &lt;strong&gt;路由 视图函数 视图&lt;/strong&gt; ,在上面那段代码中，&lt;code&gt;test&lt;/code&gt; 就是视图函数。&lt;/p&gt;

&lt;p&gt;这是一段执行逻辑，比如客户端请求 &lt;code&gt;0.0.0.0:8000/&lt;/code&gt; 此时返回的内容就是由&lt;code&gt;test&lt;/code&gt; 这个视图函数提供的。&lt;/p&gt;

&lt;p&gt;在我看来，视图函数就是一个纽带，它起到承上启下的作用，那么，到底是怎样的承上启下呢？让我们结合代码(&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoner/blob/master/docs/part2/1.Sanic%E6%BA%90%E7%A0%81%E9%98%85%E8%AF%BB-%E5%9F%BA%E4%BA%8E0.1.2.md&#34;&gt;sanic0.1.2源码&lt;/a&gt;)来分析下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;
@app.route(&amp;quot;/&amp;quot;)
async def test(request):
    return text(&#39;Hello World!&#39;)

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;这个路由装饰器的作用很简单，就是将 &lt;code&gt;/&lt;/code&gt; 这个 &lt;code&gt;uri&lt;/code&gt; 与视图函数&lt;code&gt;test&lt;/code&gt;关联起来，或许你可以将路由想象成一个 &lt;code&gt;dict&lt;/code&gt;，当客户端若请求 &lt;code&gt;0.0.0.0:8000/&lt;/code&gt;，路由就会&lt;code&gt;get&lt;/code&gt; &lt;code&gt;/&lt;/code&gt; 对应的视图函数&lt;code&gt;test&lt;/code&gt;，然后执行。&lt;/p&gt;

&lt;p&gt;其实真实情况和我们想象的差不多，请看 &lt;code&gt;sanic.py&lt;/code&gt; 中的第三十行:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;
# Decorator
def route(self, uri, methods=None):

    def response(handler):
        # 路由类的add 方法将视图函数handler 与uri 关联起来
        # 然后整个路由列表会新增一个 namedtuple 如下：
        # Route(handler=handler, methods=methods_dict, pattern=pattern, parameters=parameters)
        self.router.add(uri=uri, methods=methods, handler=handler)
        return handler

    return response

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;此时，路由就和 &lt;code&gt;uri&lt;/code&gt; 对应的视图函数关联起来了，这就是承上，路由和视图函数就是这样对应的关系。
103行有个&lt;code&gt;handle_request&lt;/code&gt;函数：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;
async def handle_request(self, request, response_callback):
    &amp;quot;&amp;quot;&amp;quot;
    Takes a request from the HTTP Server and returns a response object to be sent back
    The HTTP Server only expects a response object, so exception handling must be done here
    :param request: HTTP Request object
    :param response_callback: Response function to be called with the response as the only argument
    :return: Nothing
    &amp;quot;&amp;quot;&amp;quot;

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;当服务器监听到某个请求的时候，&lt;code&gt;handle_request&lt;/code&gt;可以通过参数中的&lt;code&gt;request.url&lt;/code&gt; 来找到视图函数并且执行，随即生成视图返回，这便是所谓的启下。&lt;/p&gt;

&lt;p&gt;其实浏览器显示的内容就是我们所谓的视图，视图是视图函数生成的，其中Jinja2起到模板渲染的作用。&lt;/p&gt;

&lt;h3 id=&#34;蓝图&#34;&gt;蓝图&lt;/h3&gt;

&lt;p&gt;到这里，你一定已经很明白sanic框架是怎么处理一个请求并将视图返回给客户端，然后也掌握了如何编写自己定义的模板(html)以及样式(css)，通过前面一节&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer/blob/master/docs/part1/3.%E9%A1%B9%E7%9B%AE%E7%BB%93%E6%9E%84.md&#34;&gt;项目结构&lt;/a&gt;的介绍，我们可以总结出如下经验：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;对于css、js等静态文件，常规操作是将其放在自己建立的statics下面&lt;/li&gt;
&lt;li&gt;对于html模板，常规操作是将其放在自己建立的templates下面&lt;/li&gt;
&lt;li&gt;视图函数(即服务的逻辑实现)放在自己建立的views下面&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;是时候考虑以下这种情况了，你需要编写一个比较复杂的http服务，你需要定义几百个路由，在编写过程中你会发现有许多不顺心的地方：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;各种不同类型的路由互相交杂在一起，命名困难，管理困难&lt;/li&gt;
&lt;li&gt;不同页面的css文件同样堆积在一起，html也是如此&lt;/li&gt;
&lt;li&gt;&amp;hellip;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;实在是令人烦恼，可能你想要一个模块化的编写方式，一个文件编写后台，url统一是&lt;code&gt;/admin/***&lt;/code&gt;，一个文件编写发帖，&lt;code&gt;/post/***&lt;/code&gt; 等等，各个文件下面url自动会带上自定义的前缀，不用考虑命名问题，不用重复写url前缀，多么美好&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Blueprint&lt;/strong&gt;，就是sanic为你提供的解决方案，依然是上节rss的例子，让我们利用Blueprint来简单实现一下我们的需求，比如我们要构建的网站分为两个部分：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;code&gt;/json/index&lt;/code&gt; 返回json格式的数据&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/html/index&lt;/code&gt; 返回html视图&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;我们在上节代码的基础上添加Blueprint，先看看定好的项目结构：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;
.
├── __init__.py
├── config
│   ├── __init__.py
│   ├── config.py
│   ├── dev_config.py
│   └── pro_config.py
├── run.py
├── statics
│   ├── rss_html # rss_html蓝图的 css js 文件存放目录
│   │   ├── css
│   │   │   └── main.css
│   │   └── js
│   └── rss_json # rss_json蓝图的 css js 文件存放目录
├── templates
│   ├── rss_html # rss_html蓝图的 html 文件存放目录
│   │   ├── index.html
│   │   └── rss.html
│   └── rss_json # rss_json蓝图的 html 文件存放目录
│       └── index.html
└── views
    ├── __init__.py
    ├── rss_html.py # rss_html 蓝图
    └── rss_json.py # rss_json 蓝图

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;蓝图的目的就是让我们构建的项目灵活，可扩展，容易阅读，方便管理，一个个小的蓝图就构建了一个大型的项目，run.py里面可以随意组合注册蓝图，可抽插式的构建我们的服务，&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer/tree/master/examples/demo04/sample01&#34;&gt;sample01&lt;/a&gt;里是本次示例的代码，建议先读一遍，十分简单，主要就是讲路由根据你自己定义的特性分割成一个个蓝图，比如这里的&lt;code&gt;rss_html&lt;/code&gt;和&lt;code&gt;rss_json&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;
#!/usr/bin/env python
# 部分代码
# rss_html.py
import sys

from sanic import Blueprint
from sanic.response import html

from src.config import CONFIG


html_bp = Blueprint(&#39;rss_html&#39;, url_prefix=&#39;html&#39;)
html_bp.static(&#39;/statics/rss_html&#39;, CONFIG.BASE_DIR + &#39;/statics/rss_html&#39;)

@html_bp.route(&amp;quot;/&amp;quot;)
async def index(request):
    return await template(&#39;index.html&#39;)

#!/usr/bin/env python
# 部分代码
# rss_json.py
import sys

from sanic import Blueprint
from sanic.response import html

from src.config import CONFIG


json_bp = Blueprint(&#39;rss_json&#39;, url_prefix=&#39;json&#39;)
json_bp.static(&#39;/statics/rss_json&#39;, CONFIG.BASE_DIR + &#39;/statics/rss_json&#39;)

@json_bp.route(&amp;quot;/&amp;quot;)
async def index(request):
    return await template(&#39;index.html&#39;)

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;不知你是否感受到这样编写服务的好处，让我们列举下：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;每个蓝图都有其自己定义的前缀，如 /html/ /json/&lt;/li&gt;
&lt;li&gt;不同蓝图下面route可相同&lt;/li&gt;
&lt;li&gt;html以及css等 根据蓝图名称目录引用&lt;/li&gt;
&lt;li&gt;模块化&lt;/li&gt;
&lt;li&gt;&amp;hellip;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;不多说，看运行效果：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;
cd /Sanic-For-Pythoneer/examples/demo04/sample01/src
python run.py

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;现在，请访问：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;http://0.0.0.0:8000/html/&#34;&gt;http://0.0.0.0:8000/html/&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;http://0.0.0.0:8000/json/&#34;&gt;http://0.0.0.0:8000/json/&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;http://0.0.0.0:8000/html/index&#34;&gt;http://0.0.0.0:8000/html/index&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;http://0.0.0.0:8000/json/index&#34;&gt;http://0.0.0.0:8000/json/index&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;感觉这个设计方便了全世界^_^&lt;/p&gt;

&lt;h3 id=&#34;说明&#34;&gt;说明&lt;/h3&gt;

&lt;p&gt;代码地址：&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer/tree/master/examples/demo04/&#34;&gt;demo04&lt;/a&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Sanic 源码阅读 - 基于0.1.2</title>
      <link>https://www.howie6879.cn/post/31/</link>
      <pubDate>Wed, 20 Dec 2017 10:10:49 +0000</pubDate>
      
      <guid>https://www.howie6879.cn/post/31/</guid>
      <description>

&lt;p&gt;&lt;code&gt;Sanic&lt;/code&gt;是一个可以使用&lt;code&gt;async/await&lt;/code&gt;语法编写项目的异步非阻塞框架，它写法类似于&lt;code&gt;Flask&lt;/code&gt;，但使用了异步特性，而且还使用&lt;code&gt;uvloop&lt;/code&gt;作为事件循环，其底层使用的是&lt;code&gt;libuv&lt;/code&gt;，从而使 &lt;code&gt;Sanic&lt;/code&gt;的速度优势更加明显。&lt;/p&gt;

&lt;p&gt;如果你：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;想深入了解Sanic，迫切想知道它的运行机制&lt;/li&gt;
&lt;li&gt;直接阅读源码，做一些定制&lt;/li&gt;
&lt;li&gt;学习&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;将Sanic-0.1.2阅读完后，我觉得你应该有以下基础再阅读源码才会理解地比较好：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;理解&lt;a href=&#34;http://blog.howie6879.cn/2016/09/28/04/&#34;&gt;装饰器&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;理解协程&lt;/li&gt;
&lt;/ul&gt;

&lt;!--- more ---&gt;

&lt;p&gt;Sanic-0.1.2 的核心文件如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;.
├── __init__.py
├── blueprints.py
├── config.py
├── exceptions.py
├── log.py
├── request.py
├── response.py
├── router.py
├── sanic.py
├── server.py
└── utils.py

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;通过运行下面的示例，这些文件都会被我们看到它的作用，拭目以待吧。&lt;/p&gt;

&lt;h3 id=&#34;simple-server-py&#34;&gt;simple_server.py&lt;/h3&gt;

&lt;p&gt;让我们从&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/examples/simple_server.py&#34;&gt;simple_server&lt;/a&gt;开始吧，代码如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;
from sanic_0_1_2.src import Sanic
from sanic_0_1_2.src.response import json

app = Sanic(__name__)


@app.route(&amp;quot;/&amp;quot;)
async def test(request):
    return json({&amp;quot;test&amp;quot;: True})


app.run(host=&amp;quot;0.0.0.0&amp;quot;, port=8000)

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;或许你直接把&lt;a href=&#34;https://github.com/howie6879/sanic_annotation&#34;&gt;sanic_annotation&lt;/a&gt;项目直接clone到本地比较方便调试+理解：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;git clone https://github.com/howie6879/sanic_annotation
cd sanic_annotation/sanic_0_1_2/examples/
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;那么，现在一切准备就绪，开始阅读吧。&lt;/p&gt;

&lt;p&gt;前两行代码导入包：
- &lt;code&gt;Sanic&lt;/code&gt;：构建一个 Sanic 服务必须要实例化的类
- &lt;code&gt;json&lt;/code&gt;：以json格式返回结果，实际上是HTTPResponse类，根据实例化参数content_type的不同，构建不同的实例，如：
    - &lt;code&gt;text&lt;/code&gt;：&lt;code&gt;content_type=&amp;quot;text/plain; charset=utf-8&amp;quot;&lt;/code&gt;
    - &lt;code&gt;html&lt;/code&gt;：&lt;code&gt;content_type=&amp;quot;text/html; charset=utf-8&amp;quot;&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;实例化一个&lt;code&gt;Sanic&lt;/code&gt;对象，&lt;code&gt;app = Sanic(__name__)&lt;/code&gt;，可见&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/src/sanic.py&#34;&gt;sanic.py&lt;/a&gt;，我已经在这个文件里面做了一些注释，这里也详细说下&lt;code&gt;Sanic&lt;/code&gt;类：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;p&gt;route()：装饰器，构建uri和视图函数的映射关系，调用Router().add()方法&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;exception()：装饰器，和上面差不多，不过针对的是错误处理类Handler&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;middleware()：装饰器，针对中间件&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;register_blueprint()：注册视图的函数，接受第一个参数是视图类&lt;code&gt;blueprint&lt;/code&gt;，再调用该类下的&lt;code&gt;register&lt;/code&gt;方法实现将此蓝图下的&lt;code&gt;route、exception、middleware&lt;/code&gt;统一注册到&lt;code&gt;app.route、app.exception、app.exception&lt;/code&gt;&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;handle_request()：这是一个很重要的异步函数，当服务启动后，如果客户端发来一个有效的请求，会自动执行 &lt;code&gt;on_message_complete&lt;/code&gt;函数，该函数的目的是异步调用 &lt;code&gt;handle_request&lt;/code&gt;函数，&lt;code&gt;handle_request&lt;/code&gt;函数会回调&lt;code&gt;write_response&lt;/code&gt;函数，&lt;code&gt;write_response&lt;/code&gt;接受的参数是此uri请求对应的视图函数，比如上面demo中，如果客户端请求&amp;rsquo;/&amp;lsquo;，那么这里&lt;code&gt;write_response&lt;/code&gt;就会接受&lt;code&gt;json({&amp;quot;test&amp;quot;: True})&lt;/code&gt;，然后进一步处理，再返回给客户端&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;run()：Sanic服务的启动函数，必须执行，实际上会继续调用&lt;code&gt;server.serve&lt;/code&gt;函数，详情下面会详细讲&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;stop()：终止服务&lt;/p&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;其实上面这部分介绍已经讲了Sanic基本的运行逻辑，如果你理解了，那下面的讲解对你来说是轻轻松松，如果不怎么明白，也不要紧，这是只是一个大体的介绍，跟着步骤来，也很容易理解，继续看代码：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# 此处将路由 / 与视图函数 test 关联起来
@app.route(&amp;quot;/&amp;quot;)
async def test(request):
    return json({&amp;quot;test&amp;quot;: True})

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;app.route&lt;/code&gt;，上面介绍过，随着Sanic服务的启动而启动，可定义参数&lt;code&gt;uri, methods&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;目的是为&lt;code&gt;url&lt;/code&gt;的&lt;code&gt;path&lt;/code&gt;和视图函数对应起来，构建一对映射关系，本例中&lt;code&gt;Sanic.router&lt;/code&gt;类下的&lt;code&gt;Router.routes = []&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;会增加一个名为&lt;code&gt;Route&lt;/code&gt;的&lt;code&gt;namedtuple&lt;/code&gt;，如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;[Route(handler=&amp;lt;function test at 0x10a0f6488&amp;gt;, methods=None, pattern=re.compile(&#39;^/$&#39;), parameters=[])]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;看到没，&lt;code&gt;uri &#39;/&#39;&lt;/code&gt; 和视图函数&lt;code&gt;test&lt;/code&gt;对应起来了，如果客户端请求&lt;code&gt;&#39;/&#39;&lt;/code&gt;，当服务器监听到这个请求的时候,&lt;code&gt;handle_request&lt;/code&gt;可以通过参数中的&lt;code&gt;request.url&lt;/code&gt;来找到视图函数&lt;code&gt;test&lt;/code&gt;并且执行，随即生成视图返回&lt;/p&gt;

&lt;p&gt;那么这里&lt;code&gt;write_response&lt;/code&gt;就会接受视图函数test返回的&lt;code&gt;json({&amp;quot;test&amp;quot;: True})&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;说下&lt;code&gt;Router&lt;/code&gt;类，这个类的目的就是添加和获取路由对应的视图函数，把它想象成&lt;code&gt;dict&lt;/code&gt;或许更容易理解：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;add(self, uri, methods, handler)：添加一个映射关系到self.routes&lt;/li&gt;
&lt;li&gt;get(self, request)：获取request.url对应的视图函数&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;最后一行，&lt;code&gt;app.run(host=&amp;quot;0.0.0.0&amp;quot;, port=8000)&lt;/code&gt;，Sanic 下的&lt;code&gt;run&lt;/code&gt;函数，启动一个&lt;code&gt;http server&lt;/code&gt;，主要是启动&lt;code&gt;run&lt;/code&gt;里面的&lt;code&gt;serve&lt;/code&gt;函数，参数如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;
try:
    serve(
        host=host,
        port=port,
        debug=debug,
        # 服务开始后启动的函数
        after_start=after_start,
        # 在服务关闭前启动的函数
        before_stop=before_stop,
        # Sanic(__name__).handle_request()
        request_handler=self.handle_request,
        # 默认读取Config
        request_timeout=self.config.REQUEST_TIMEOUT,
        request_max_size=self.config.REQUEST_MAX_SIZE,
    )
except:
    pass

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;让我们将目光投向&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/src/server.py&#34;&gt;server.py&lt;/a&gt;，这也是Sanic框架的核心代码：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;p&gt;serve()：里面会创建一个TCP服务的协程，然后通过&lt;code&gt;loop.run_forever()&lt;/code&gt;运行这个事件循环，以便接收客户端请求以及处理相关事件，每当一个新的客户端建立连接服务就会创建一个新的&lt;code&gt;Protocol&lt;/code&gt;实例，接受请求与返回响应离不开其中的&lt;code&gt;HttpProtocol&lt;/code&gt;，里面的函数支持接受数据、处理数据、执行视图函数、构建响应数据并返回给客户端&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;HttpProtocol：&lt;code&gt;asyncio.Protocol&lt;/code&gt;的子类，用来处理与客户端的通信，我在&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/src/server.py&#34;&gt;server.py&lt;/a&gt;里写了对应的注释&lt;/p&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;至此，Sanic 服务启动了&lt;/p&gt;

&lt;p&gt;不要小看这一个小小的demo，执行一下，竟然涉及到下面这么多个文件，让我们总结一下：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;p&gt;&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/src/sanic.py&#34;&gt;sanic.py&lt;/a&gt;&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/src/server.py&#34;&gt;server.py&lt;/a&gt;&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/src/router.py&#34;&gt;router.py&lt;/a&gt;&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/src/request.py&#34;&gt;request.py&lt;/a&gt;&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/src/response.py&#34;&gt;response.py&lt;/a&gt;&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/src/exceptions.py&#34;&gt;exceptions.py&lt;/a&gt;&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/src/config.py&#34;&gt;config.py&lt;/a&gt;&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/src/log.py&#34;&gt;log.py&lt;/a&gt;&lt;/p&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;除去&lt;code&gt;__init__.py&lt;/code&gt;，&lt;code&gt;Sanic&lt;/code&gt;项目一共就10个文件，这个小demo不显山不露水地竟然用到了8个，虽然其中几个没有怎么用到，但也足够说明，你如果理解了这个demo，&lt;code&gt;Sanic&lt;/code&gt;的运行逻辑以及框架代码你已经了解地很深入了&lt;/p&gt;

&lt;h3 id=&#34;blueprints-py&#34;&gt;blueprints.py&lt;/h3&gt;

&lt;p&gt;这个例子看完，我们就能轻易地明白什么是&lt;code&gt;blueprints&lt;/code&gt;，以及&lt;code&gt;blueprints&lt;/code&gt;的运行方式，代码如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;
from sanic_0_1_2.src import Sanic
# 引入Blueprint
from sanic_0_1_2.src import Blueprint
from sanic_0_1_2.src.response import json, text

app = Sanic(__name__)
blueprint = Blueprint(&#39;name&#39;, url_prefix=&#39;/my_blueprint&#39;)
blueprint2 = Blueprint(&#39;name2&#39;, url_prefix=&#39;/my_blueprint2&#39;)


@blueprint.route(&#39;/foo&#39;)
async def foo(request):
    return json({&#39;msg&#39;: &#39;hi from blueprint&#39;})


@blueprint2.route(&#39;/foo&#39;)
async def foo2(request):
    return json({&#39;msg&#39;: &#39;hi from blueprint2&#39;})


app.register_blueprint(blueprint)
app.register_blueprint(blueprint2)

app.run(host=&amp;quot;0.0.0.0&amp;quot;, port=8000, debug=True)

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;让我们从这两行开始：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;
blueprint = Blueprint(&#39;name&#39;, url_prefix=&#39;/my_blueprint&#39;)
blueprint2 = Blueprint(&#39;name2&#39;, url_prefix=&#39;/my_blueprint2&#39;)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;显然，&lt;code&gt;blueprint&lt;/code&gt;以及&lt;code&gt;blueprint2&lt;/code&gt;是&lt;code&gt;Blueprint&lt;/code&gt;根据不同的参数生成的不同的实例对象，接下来要干嘛？没错，分析&lt;a href=&#34;https://github.com/howie6879/sanic_annotation/blob/master/sanic_0_1_2/src/blueprints.py&#34;&gt;blueprints.py&lt;/a&gt;:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;BlueprintSetup：蓝图注册类

&lt;ul&gt;
&lt;li&gt;add_route：添加路由到app&lt;/li&gt;
&lt;li&gt;add_exception：添加对应抛出的错误到app&lt;/li&gt;
&lt;li&gt;add_middleware：添加中间件到app&lt;/li&gt;
&lt;/ul&gt;&lt;/li&gt;
&lt;li&gt;Blueprint：蓝图类，接收两个参数：name(蓝图名称) url_prefix 该蓝图的url前缀

&lt;ul&gt;
&lt;li&gt;route：路由装饰器，将会生成一个匿名函数到self.deferred_functions列表里稍后一起处理注册到app里&lt;/li&gt;
&lt;li&gt;middleware：同上&lt;/li&gt;
&lt;li&gt;exception：同上&lt;/li&gt;
&lt;li&gt;record：注册一个回调函数到self.deferred_functions列表里面，&lt;/li&gt;
&lt;li&gt;make_setup_state：实例化BlueprintSetup&lt;/li&gt;
&lt;li&gt;register：注册视图，实际就是注册route、middleware、exception到app，此时会利用make_setup&lt;em&gt;state返回的BlueprintSetup示例进行对于的add&lt;/em&gt;***一系列操作，相当于Sanic().route()效果&lt;/li&gt;
&lt;/ul&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;请看下&lt;code&gt;route&lt;/code&gt;和&lt;code&gt;register&lt;/code&gt;函数，然后再看下面的代码：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# 生成一个匿名函数到self.deferred_functions列表里 包含三个参数 handler(foo), uri, methods
@blueprint.route(&#39;/foo&#39;)
async def foo(request):
    return json({&#39;msg&#39;: &#39;hi from blueprint&#39;})


@blueprint2.route(&#39;/foo&#39;)
async def foo2(request):
    return json({&#39;msg&#39;: &#39;hi from blueprint2&#39;})

# 上一个例子说过这个函数，Sanic().register_blueprint() 注册蓝图
app.register_blueprint(blueprint)
app.register_blueprint(blueprint2)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;怎么样，现在来看，是不是很轻松，这一行&lt;code&gt;app.run(host=&amp;quot;0.0.0.0&amp;quot;, port=8000, debug=True)&lt;/code&gt;服务启动代码不用多说吧？&lt;/p&gt;

&lt;h3 id=&#34;总结&#34;&gt;总结&lt;/h3&gt;

&lt;p&gt;看到这里，相信你已经完全理解了Sanic的运行机制，虽然还有&lt;code&gt;middleware&amp;amp;exception&lt;/code&gt;的注册以及调用机制没讲，但这和&lt;code&gt;route&lt;/code&gt;的运行机制一样，如果你懂了&lt;code&gt;route&lt;/code&gt;那么这两个也很简单&lt;/p&gt;

&lt;p&gt;本人技术微末，若有错误，请指出，不胜感激&lt;/p&gt;

&lt;p&gt;注解地址：&lt;a href=&#34;https://github.com/howie6879/sanic_annotation&#34;&gt;sanic_annotation&lt;/a&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Sanic 使用教程 - 3.项目结构</title>
      <link>https://www.howie6879.cn/post/30/</link>
      <pubDate>Fri, 06 Oct 2017 08:04:38 +0000</pubDate>
      
      <guid>https://www.howie6879.cn/post/30/</guid>
      <description>

&lt;p&gt;一个项目，在最外层他们应该是一样的，至少需要有:&lt;/p&gt;

&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th align=&#34;center&#34;&gt;文件夹&lt;/th&gt;
&lt;th align=&#34;center&#34;&gt;说明&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;

&lt;tbody&gt;
&lt;tr&gt;
&lt;td align=&#34;center&#34;&gt;docs&lt;/td&gt;
&lt;td align=&#34;center&#34;&gt;项目文档说明&lt;/td&gt;
&lt;/tr&gt;

&lt;tr&gt;
&lt;td align=&#34;center&#34;&gt;src/pro_name&lt;/td&gt;
&lt;td align=&#34;center&#34;&gt;项目名称&lt;/td&gt;
&lt;/tr&gt;

&lt;tr&gt;
&lt;td align=&#34;center&#34;&gt;tests&lt;/td&gt;
&lt;td align=&#34;center&#34;&gt;测试用例&lt;/td&gt;
&lt;/tr&gt;

&lt;tr&gt;
&lt;td align=&#34;center&#34;&gt;README.md&lt;/td&gt;
&lt;td align=&#34;center&#34;&gt;项目介绍&lt;/td&gt;
&lt;/tr&gt;

&lt;tr&gt;
&lt;td align=&#34;center&#34;&gt;requirements.txt&lt;/td&gt;
&lt;td align=&#34;center&#34;&gt;该项目依赖的第三方库&lt;/td&gt;
&lt;/tr&gt;

&lt;tr&gt;
&lt;td align=&#34;center&#34;&gt;…&amp;hellip;&lt;/td&gt;
&lt;td align=&#34;center&#34;&gt;…&amp;hellip;&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;那接下来需要讨论的，就是 &lt;code&gt;src&lt;/code&gt; 的内部结构该是什么样的呢？&lt;/p&gt;

&lt;!-- more --&gt;

&lt;p&gt;本章将写一个 &lt;code&gt;rss&lt;/code&gt; 解析展示的项目用做演示。&lt;/p&gt;

&lt;h3 id=&#34;2-1-普通的项目结构&#34;&gt;2.1.普通的项目结构&lt;/h3&gt;

&lt;p&gt;一个普通的项目：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;不需要添加后续模块功能&lt;/li&gt;
&lt;li&gt;快速开发使用，不需要维护&lt;/li&gt;
&lt;li&gt;无较复杂的前端需求&lt;/li&gt;
&lt;li&gt;用完就走&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;那么就可以像 &lt;code&gt;demo01&lt;/code&gt; 中一样，只需要添加一个 &lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoner/blob/master/examples/demo03/sample01/src/run.py&#34;&gt;run.py&lt;/a&gt; 或者叫做 &lt;code&gt;app.py&lt;/code&gt; 文件（反正这是一个启动文件，命名可随意），不论是配置、路由都写在一起就好了。&lt;/p&gt;

&lt;p&gt;新建一个项目如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;sample01
├── docs
│   └── demo.md
├── src
│   └── run.py
├── tests
├── .gitignore
└──requirements.txt
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;任意一个 &lt;code&gt;rss&lt;/code&gt; 源，假设项目需要将其中的文章标题以及链接提取并展示出来，比如以json格式返回，这属于很简单的功能，可以说只有一段逻辑，&lt;code&gt;run.py&lt;/code&gt; 内容如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;#!/usr/bin/env python
from sanic import Sanic
from sanic.response import json
from feedparser import parse

app = Sanic()


@app.route(&amp;quot;/&amp;quot;)
async def index(request):
    url = &amp;quot;http://blog.howie6879.cn/atom.xml&amp;quot;
    feed = parse(url)
    articles = feed[&#39;entries&#39;]
    data = []
    for article in articles:
        data.append({&amp;quot;title&amp;quot;: article[&amp;quot;title_detail&amp;quot;][&amp;quot;value&amp;quot;], &amp;quot;link&amp;quot;: article[&amp;quot;link&amp;quot;]})
    return json(data)


if __name__ == &amp;quot;__main__&amp;quot;:
    app.run(host=&amp;quot;0.0.0.0&amp;quot;, port=8000)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;访问 &lt;code&gt;http://0.0.0.0:8000/&lt;/code&gt;，会返回一串json，如下：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-10-06-03-01.png-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;接下来，问题升级，我想要将标题链接用页面展示，该怎么弄？&lt;/p&gt;

&lt;p&gt;我们需要一个页面模板，然后写入json数据，最后用 &lt;code&gt;jinja2&lt;/code&gt; 的 &lt;code&gt;template&lt;/code&gt; 将其渲染：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# template的渲染模板
# 后面会使用更方便的模板引用方式
template = Template(
    &amp;quot;&amp;quot;&amp;quot;
    &amp;lt;!DOCTYPE html&amp;gt;
&amp;lt;html lang=&amp;quot;en&amp;quot;&amp;gt;
&amp;lt;head&amp;gt;
    &amp;lt;meta charset=&amp;quot;UTF-8&amp;quot;&amp;gt;
    &amp;lt;title&amp;gt;rss阅读&amp;lt;/title&amp;gt;
    &amp;lt;meta name=&amp;quot;viewport&amp;quot; content=&amp;quot;width=device-width, initial-scale=1&amp;quot;&amp;gt;
&amp;lt;/head&amp;gt;
&amp;lt;body&amp;gt;
&amp;lt;article class=&amp;quot;markdown-body&amp;quot;&amp;gt;
    {% for article in articles %}
    &amp;lt;b&amp;gt;&amp;lt;a href=&amp;quot;{{article.link}}&amp;quot;&amp;gt;{{article.title}}&amp;lt;/a&amp;gt;&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;
    &amp;lt;i&amp;gt;{{article.published}}&amp;lt;/i&amp;gt;&amp;lt;br/&amp;gt;
    &amp;lt;hr/&amp;gt;
    {% endfor %}
&amp;lt;/article&amp;gt;
&amp;lt;/body&amp;gt;
&amp;lt;/html&amp;gt;
    &amp;quot;&amp;quot;&amp;quot;
)

# 逻辑函数
@app.route(&amp;quot;/html&amp;quot;)
async def rss_html(request):
    url = &amp;quot;http://blog.howie6879.cn/atom.xml&amp;quot;
    feed = parse(url)
    articles = feed[&#39;entries&#39;]
    data = []
    for article in articles:
        data.append(
            {&amp;quot;title&amp;quot;: article[&amp;quot;title_detail&amp;quot;][&amp;quot;value&amp;quot;], &amp;quot;link&amp;quot;: article[&amp;quot;link&amp;quot;], &amp;quot;published&amp;quot;: article[&amp;quot;published&amp;quot;]})
    html_content = template.render(articles=data)
    return html(html_content)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;具体结构代码见&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoner/tree/master/examples/demo03/sample01&#34;&gt;sample01&lt;/a&gt;，运行起来，然后输入 &lt;code&gt;http://0.0.0.0:8000/html&lt;/code&gt; 就可以看到被展示出来的页面^_^&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-10-06-03-02.png-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;假设需要编写前端页面比较多，那么你就需要添加&lt;code&gt;static&lt;/code&gt; 以及 &lt;code&gt;templates&lt;/code&gt; 文件夹用来管理各个界面模块，具体下面会介绍。&lt;/p&gt;

&lt;h3 id=&#34;2-2-项目结构具体说明&#34;&gt;2.2.项目结构具体说明&lt;/h3&gt;

&lt;p&gt;当编写的项目过于复杂，我都会将其当做一个第三方包来管理项目中涉及的各种模块，比如 &lt;code&gt;owllook&lt;/code&gt;，目录下面你会发现有个 &lt;code&gt;__init__.py&lt;/code&gt; 文件，它初始化了当前目录下的应用，然后代码中引用某个函数可以这么写：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;from owllook.config import Config
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;每个项目的内部分布以及命名可能不一样（甚至目录比应该或多或少），但大体意思可能差不多，下面介绍本次项目 &lt;code&gt;src&lt;/code&gt; 下的一些文件目录结构：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;sample02
├── docs
│   └── demo.md
├── src
│   ├── config # 配置 
│   ├── static # css、js、img
│   ├── templates # Jinja2模板
│   └── views # 路由、逻辑处理
│   ├── __init__.py
│   ├── run.py # 启动文件
├── tests
└── requirements.txt
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;此处就可以将 &lt;code&gt;sample02&lt;/code&gt; 当成一个包了，咱们来试试看：&lt;/p&gt;

&lt;p&gt;首先新建文件 &lt;code&gt;/views/rss.py&lt;/code&gt; :&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;enable_async = sys.version_info &amp;gt;= (3, 6)

app = Sanic()

# jinjia2 config
env = Environment(
    loader=PackageLoader(&#39;views.rss&#39;, &#39;../templates&#39;),
    autoescape=select_autoescape([&#39;html&#39;, &#39;xml&#39;, &#39;tpl&#39;]),
    enable_async=enable_async)


async def template(tpl, **kwargs):
    template = env.get_template(tpl)
    rendered_template = await template.render_async(**kwargs)
    return html(rendered_template)


@app.route(&amp;quot;/html&amp;quot;)
async def rss_html(request):
    url = &amp;quot;http://blog.howie6879.cn/atom.xml&amp;quot;
    feed = parse(url)
    articles = feed[&#39;entries&#39;]
    data = []
    for article in articles:
        data.append(
            {&amp;quot;title&amp;quot;: article[&amp;quot;title_detail&amp;quot;][&amp;quot;value&amp;quot;], &amp;quot;link&amp;quot;: article[&amp;quot;link&amp;quot;], &amp;quot;published&amp;quot;: article[&amp;quot;published&amp;quot;]})
    return await template(&#39;rss.html&#39;, articles=articles)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;这里使用异步的方式引入了 &lt;code&gt;jinja2&lt;/code&gt; ，需要注意的是python版本必须3.6+，否则就得使用同步的方式来引入 &lt;code&gt;jinja2&lt;/code&gt; ，后面章节会继续介绍。&lt;/p&gt;

&lt;p&gt;此时启动文件 &lt;code&gt;run.py&lt;/code&gt; 只要引入 &lt;code&gt;/views/rss.py&lt;/code&gt; 的 &lt;code&gt;app&lt;/code&gt; 实例即可：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# !/usr/bin/env python
import sys
import os

sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from src.views import app
from src.config import CONFIG

app.static(&#39;/static&#39;, CONFIG.BASE_DIR + &#39;/static&#39;)

if __name__ == &amp;quot;__main__&amp;quot;:
    app.run(host=&amp;quot;0.0.0.0&amp;quot;, port=8000)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;还有一些css文件这里就不介绍，具体代码请看&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoner/tree/master/examples/demo03/sample02&#34;&gt;sample02&lt;/a&gt;，运行起来，然后输入 &lt;code&gt;http://0.0.0.0:8000/html&lt;/code&gt; 就好，效果如下图：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-10-06-03-03.png-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;h3 id=&#34;2-3-说明&#34;&gt;2.3.说明&lt;/h3&gt;

&lt;p&gt;关于 &lt;code&gt;views templates static&lt;/code&gt; 内部的构造也是值得一些，这就涉及到蓝图 &lt;code&gt;Blueprint&lt;/code&gt; ，后面介绍蓝图的时候会进行介绍。&lt;/p&gt;

&lt;p&gt;代码地址：&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoner/tree/master/examples/demo03/&#34;&gt;demo03&lt;/a&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Sanic 使用教程 - 2.配置</title>
      <link>https://www.howie6879.cn/post/29/</link>
      <pubDate>Fri, 06 Oct 2017 08:04:29 +0000</pubDate>
      
      <guid>https://www.howie6879.cn/post/29/</guid>
      <description>&lt;p&gt;对于一个项目来说，配置是一个很严肃的问题，比如说：在开发环境和生产环境中，配置是不同的，那么一个项目该如何自由地在不同的配置环境中进行切换呢，思考下，然后带着答案或者疑问往下阅读。&lt;/p&gt;

&lt;p&gt;新建文件夹 &lt;code&gt;demo2&lt;/code&gt; ，内部建立这样的文件结构：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;demo02
├── config
│   ├── __init__.py
│   └── config.py
└── run.py
&lt;/code&gt;&lt;/pre&gt;

&lt;!-- more --&gt;

&lt;p&gt;其中 &lt;code&gt;run.py&lt;/code&gt; 内容如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;#!/usr/bin/env python
from sanic import Sanic
from sanic.response import text

app = Sanic()


@app.route(&amp;quot;/&amp;quot;)
async def test(request):
    return text(&#39;Hello World!&#39;)


if __name__ == &amp;quot;__main__&amp;quot;:
    app.run(host=&amp;quot;0.0.0.0&amp;quot;, port=8000, debug=True)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;代码示例中开启了 &lt;code&gt;debug&lt;/code&gt; 模式，假设我们需要通过 &lt;code&gt;config.py&lt;/code&gt; 配置文件来实现控制服务的 &lt;code&gt;debug&lt;/code&gt; 模式开启与否，那该怎么实现呢。&lt;/p&gt;

&lt;p&gt;在 &lt;code&gt;config.py&lt;/code&gt; 中添加一行：&lt;code&gt;DEBUG=True&lt;/code&gt; ，然后 &lt;code&gt;run.py&lt;/code&gt; 内容改为：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;#!/usr/bin/env python
from sanic import Sanic
from sanic.response import text
from config import DEBUG

app = Sanic()


@app.route(&amp;quot;/&amp;quot;)
async def test(request):
    return text(&#39;Hello World!&#39;)


if __name__ == &amp;quot;__main__&amp;quot;:
    app.run(host=&amp;quot;0.0.0.0&amp;quot;, port=8000, debug=DEBUG)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;表面上看，功能确实实现了，但这实际上却不是很好的做法，若部署在生产环境中，难道还要特地再将 &lt;code&gt;debug&lt;/code&gt; 改为 &lt;code&gt;False&lt;/code&gt; 么，这显然很浪费时间，如果需要改变的参数有很多，那就很难维护了。&lt;/p&gt;

&lt;p&gt;那么，正确的做法应该是怎么样的呢？&lt;/p&gt;

&lt;p&gt;首先在文件夹 &lt;code&gt;demo2&lt;/code&gt; ，内部建立这样的文件结构：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;demo02
├── config
│   ├── __init__.py
│   ├── config.py
│   ├── dev_config.py
│   └── pro_config.py
└── run.py

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;然后使用类继承的方式使这三个配置文件联系起来，比如在 &lt;code&gt;config.py&lt;/code&gt; 中就只放公有配置，如：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;#!/usr/bin/env python
import os


class Config():
    &amp;quot;&amp;quot;&amp;quot;
    Basic config for demo02
    &amp;quot;&amp;quot;&amp;quot;
    # Application config
    TIMEZONE = &#39;Asia/Shanghai&#39;
    BASE_DIR = os.path.dirname(os.path.dirname(__file__))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;而在 &lt;code&gt;pro_config.py或dev_config.py&lt;/code&gt; 中就可以自由地编写不同的配置了：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# dev_config
#!/usr/bin/env python
from .config import Config


class DevConfig(Config):
    &amp;quot;&amp;quot;&amp;quot;
    Dev config for demo02
    &amp;quot;&amp;quot;&amp;quot;

    # Application config
    DEBUG = True

# pro_config
#!/usr/bin/env python
from .config import Config


class ProConfig(Config):
    &amp;quot;&amp;quot;&amp;quot;
    Pro config for demo02
    &amp;quot;&amp;quot;&amp;quot;

    # Application config
    DEBUG = False
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;配置文件还需要根据系统环境变量的设置进行不同配置环境的切换，比如设置 &lt;code&gt;MODE&lt;/code&gt; 系统环境变量，然后可以根据其不同的值切换到不同的配置文件，因此在 &lt;code&gt;__init__.py&lt;/code&gt; 中需要这么写：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;#!/usr/bin/env python
import os


def load_config():
    &amp;quot;&amp;quot;&amp;quot;
    Load a config class
    &amp;quot;&amp;quot;&amp;quot;

    mode = os.environ.get(&#39;MODE&#39;, &#39;DEV&#39;)
    try:
        if mode == &#39;PRO&#39;:
            from .pro_config import ProConfig
            return ProConfig
        elif mode == &#39;DEV&#39;:
            from .dev_config import DevConfig
            return DevConfig
        else:
            from .dev_config import DevConfig
            return DevConfig
    except ImportError:
        from .config import Config
        return Config


CONFIG = load_config()
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;默认 &lt;code&gt;MODE&lt;/code&gt; 设置为 &lt;code&gt;DEV&lt;/code&gt;，在 &lt;code&gt;run.py&lt;/code&gt; 文件中就可以这么调用：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;#!/usr/bin/env python
from sanic import Sanic
from sanic.response import text
from config import CONFIG

app = Sanic()
app.config.from_object(CONFIG)

@app.route(&amp;quot;/&amp;quot;)
async def test(request):
    return text(&#39;Hello World!&#39;)


if __name__ == &amp;quot;__main__&amp;quot;:
    app.run(host=&amp;quot;0.0.0.0&amp;quot;, port=8000, debug=app.config[&#39;DEBUG&#39;])
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;而在生产环境的服务器上，直接通过：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;# 通过设置MODE的值进行配置文件的选择
export MODE=PRO 
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;若是利用 &lt;code&gt;supervisor&lt;/code&gt; 来启动服务，可通过添加&lt;code&gt;environment = MODE=&amp;quot;PRO&amp;quot;&lt;/code&gt; 来设置环境变量，是不是很方便呢。&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Sanic 使用教程 - 1.初使用</title>
      <link>https://www.howie6879.cn/post/28/</link>
      <pubDate>Fri, 06 Oct 2017 08:03:15 +0000</pubDate>
      
      <guid>https://www.howie6879.cn/post/28/</guid>
      <description>

&lt;p&gt;在安装Sanic之前，让我们一起来看看Python在支持异步的过程中，都经历了哪些比较重大的更新。&lt;/p&gt;

&lt;p&gt;首先是Python3.4版本引入了&lt;code&gt;asyncio&lt;/code&gt;，这让Python有了支持异步IO的标准库，而后3.5版本又提供了两个新的关键字&lt;code&gt;async/await&lt;/code&gt;，目的是为了更好地标识异步IO，让异步编程看起来更加友好，最后3.6版本更进一步，推出了稳定版的&lt;code&gt;asyncio&lt;/code&gt;，从这一系列的更新可以看出，Python社区正迈着坚定且稳重的步伐向异步编程靠近。&lt;/p&gt;

&lt;h3 id=&#34;安装&#34;&gt;安装&lt;/h3&gt;

&lt;p&gt;Sanic是一个支持 &lt;code&gt;async/await&lt;/code&gt; 语法的异步无阻塞框架，这意味着我们可以依靠其处理异步请求的新特性来提升服务性能，如果你有&lt;code&gt;Flask&lt;/code&gt;框架的使用经验，那么你可以迅速地使用&lt;code&gt;Sanic&lt;/code&gt;来构建出心中想要的应用，并且性能会提升不少，我将同一服务分别用Flask和Sanic编写，再将压测的结果进行对比，发现Sanic编写的服务大概是&lt;code&gt;Falsk&lt;/code&gt;的1.5倍。&lt;/p&gt;

&lt;p&gt;仅仅是Sanic的异步特性就让它的速度得到这么大的提升么？是的，但这个答案并不标准，更为关键的是Sanic使用了&lt;code&gt;uvloop&lt;/code&gt;作为&lt;code&gt;asyncio&lt;/code&gt;的事件循环，&lt;code&gt;uvloop&lt;/code&gt;由Cython编写，它的出现让&lt;code&gt;asyncio&lt;/code&gt;更快，快到什么程度？&lt;a href=&#34;http://codingpy.com/article/uvloop-blazing-fast-networking-with-python/&#34;&gt;这篇&lt;/a&gt;文章中有介绍，其中提出速度至少比 nodejs、gevent 和其他Python异步框架要快两倍，并且性能接近于用Go编写的程序，顺便一提，Sanic的作者就是受这篇文章影响，这才有了Sanic。&lt;/p&gt;

&lt;p&gt;怎么样？有没有激起你学习Sanic的兴趣，如果有，就让我们一起开始学习吧，在开始之前，你只需要有一台安装了Python的电脑即可。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;说明：由于Windows下暂不支持安装uvloop，故在此建议使用Mac或Linux&lt;/p&gt;
&lt;/blockquote&gt;

&lt;h4 id=&#34;虚拟环境&#34;&gt;虚拟环境&lt;/h4&gt;

&lt;p&gt;程序世界一部分是对应着现实的，在生活中，我们会在不同的环境完成不同的任务，比如在厨房做饭、卧室休息，分工极其明确。&lt;/p&gt;

&lt;p&gt;其实用Python编写应用服务也是如此，它们同样希望应用服务与开发环境是一对一的关系，这样做的好处在于，每个独立的环境都可以简洁高效地管理自身对应服务所依赖的第三方库，如若不然，各个服务都安排在同一环境，这样不仅会造成管理上的麻烦，还会使第三方库之间产生冲突。&lt;/p&gt;

&lt;p&gt;通过上面的叙述，我们是不是可以得出这样一个核心观点：&lt;strong&gt;应该在不同的环境下做不同的事&lt;/strong&gt; ，以此类推，写项目的时候，我们也需要为每个不同的项目构建一个无干扰的的环境，发散思维，总结一下：&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;不同的项目，需要为其构建不同的虚拟环境，以免互相干扰&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;构建虚拟环境的工具很多，如下：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://virtualenv.pypa.io/en/stable/&#34;&gt;virtualenv&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/pyenv/pyenv&#34;&gt;pyenv&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://www.continuum.io/downloads&#34;&gt;anaconda&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;…&amp;hellip;&lt;/p&gt;

&lt;p&gt;以上三个工具都可以快速地帮助我们构建当前需要的Python环境，如果你之前没有使用过，可直接点开链接进行下载，如果你正在使用其它的环境管理工具，也不要紧，因为不论你使用哪一种方式，我们最终目的都是针对一个新项目构建一个新的环境。&lt;/p&gt;

&lt;p&gt;安装配置好之后，简单看看官方提供的使用方法，就可以开始了，比如我本机使用的是&lt;code&gt;anaconda&lt;/code&gt; ，安装完成后可以很方便地创建一个虚拟环境，比如这里使用Python3.6来作为本书项目的默认环境：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;# 新建一个python3.6环境
conda create --name python36 python=3.6
# 安装好之后 输入下面命令进入名为python36的环境
source activate python36
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;若安装速度比较慢，可以考虑换国内源，比如 &lt;a href=&#34;https://mirrors.tuna.tsinghua.edu.cn/help/pypi/&#34;&gt;国内镜像&lt;/a&gt; ，至于为什么选择python3.6作为默认环境，一是因为Sanic只支持Python3.5+，二则是我们构建的项目最终是要在生产环境下运行的，所以建议最好安装Python3.6下稳定版本的&lt;code&gt;asyncio&lt;/code&gt;。&lt;/p&gt;

&lt;h4 id=&#34;安装sanic&#34;&gt;安装Sanic&lt;/h4&gt;

&lt;p&gt;Python安装第三方模块都是利用&lt;code&gt;pip&lt;/code&gt;工具进行安装，这里也不例外，首先进入上一步我们新建的 &lt;code&gt;python3.6&lt;/code&gt; 虚拟环境，然后安装：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# 安装Sanic，请先使用 source activate python36 进入虚拟环境
pip install sanic
# 如果不想使用uvloop和ujson 可以这样安装
SANIC_NO_UVLOOP=true SANIC_NO_UJSON=true pip install sanic
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;通过上面的命令，你就可以在 &lt;code&gt;python3.6&lt;/code&gt; 虚拟环境中安装Sanic以及其依赖的第三方库了，若想查看Sanic是否已经正确安装，可以进入终端下对应的虚拟环境，启动Python解释器，导入Sanic库：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;# 启动Python解释器
python
&amp;gt;&amp;gt;&amp;gt; import sanic
&amp;gt;&amp;gt;&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果没有出现错误，就说明你已经正确地安装了Sanic，请继续阅读下一节，了解下如何利用Sanic来构建一个Web项目吧。&lt;/p&gt;

&lt;h3 id=&#34;踏出第一步&#34;&gt;踏出第一步&lt;/h3&gt;

&lt;p&gt;我们将正式使用Sanic来构建一个web项目，让我们踏出第一步，利用Sanic来编写一个返回&lt;code&gt;Hello World!&lt;/code&gt;字符串的服务程序。&lt;/p&gt;

&lt;p&gt;新建一个文件，名为 &lt;code&gt;run.py&lt;/code&gt; :&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;#!/usr/bin/env python
from sanic import Sanic
from sanic.response import text

app = Sanic()


@app.route(&amp;quot;/&amp;quot;)
async def test(request):
    return text(&#39;Hello World!&#39;)


if __name__ == &amp;quot;__main__&amp;quot;:
    app.run(host=&amp;quot;0.0.0.0&amp;quot;, port=8000)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Sanic的目标是让编写服务更加简单易用，请看上面仅用不到10行的代码，就编写好了一个简单的Web服务，运行此文件，在浏览器输入 &lt;code&gt;http://0.0.0.0:8000&lt;/code&gt; ，出现的字符会让你回想起当年学c的恐惧^_^。&lt;/p&gt;

&lt;p&gt;如果你是第一次使用Sanic，上面的代码可能会让你产生一些困扰，不用担心，接下来，我们将一起用Sanic编写一个简单的资讯阅读的web服务，在这过程中，你将逐渐地了解到Sanic的一些基本用法，如路由的构建、接受请求数据以及返回响应的内容等。&lt;/p&gt;

&lt;p&gt;本次示例的源代码全部在github上，见&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoneer/blob/master/examples/demo01/news.py&#34;&gt;examples/demo01/news.py&lt;/a&gt;。&lt;/p&gt;

&lt;h4 id=&#34;编写一个资讯阅读项目&#34;&gt;编写一个资讯阅读项目&lt;/h4&gt;

&lt;p&gt;在开始编写之前，第一步最好写一下需求，哪怕是个简单不过的玩具项目也不能略过这个步骤，比如现在编写的资讯阅读项目，需求就一个，在页面中展示一些资讯新闻。&lt;/p&gt;

&lt;p&gt;既然是展示资讯新闻，那么解决数据来源的问题最为重要，对于这个问题你也不用担心，因为在本次示例的源码中我编写了一个名为&lt;code&gt;get_news()&lt;/code&gt;的函数专门用来返回资讯新闻数据，简化代码如下:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;async def get_news(size=10):
    &amp;quot;&amp;quot;&amp;quot;
    Sanic是一个异步框架，为了更好的发挥它的性能，有些操作最好也要用异步的
    比如这里发起请求就必须要用异步请求框架aiohttp
    所以使用本服务的时候请先执行: pip install aiohttp
    数据使用的是readhub网站的api接口
    为了使这个数据获取函数正常运行，我会保持更新，所以具体代码：examples/demo01/news.py
    &amp;quot;&amp;quot;&amp;quot;
    async with aiohttp.ClientSession() as client:
        async with client.get(readhub_api, params=params, headers=headers) as response:
            assert response.status == 200
            text = await response.json()
        return text
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;这样各位就可以只专注于Sanic的代码实现，而不必考虑其他问题，我会一直维护这个数据获取函数，以保证数据正常输出，各位请放心使用。&lt;/p&gt;

&lt;h4 id=&#34;构建路由&#34;&gt;构建路由&lt;/h4&gt;

&lt;p&gt;数据的问题解决之后，我们可以开始着手于需求的实现了，根据前面的描述，此时的需求是当客户端（Web浏览器）访问&lt;code&gt;http://0.0.0.0:8000/&lt;/code&gt;的时候，浏览器会立马展示服务端响应返回的10条资讯新闻（假设内容由index()函数返回），若浏览器访问的是&lt;code&gt;http://0.0.0.0:8000/2&lt;/code&gt;，此时返回的就是第二页的10条资讯新闻，以此类推&amp;hellip;&amp;hellip;&lt;/p&gt;

&lt;p&gt;当Sanic程序实例接收到一个请求，比如前面提到的&lt;code&gt;http://0.0.0.0:8000/&lt;/code&gt;，它是怎么知道这个URL可以对应到&lt;code&gt;index()&lt;/code&gt;函数呢？&lt;/p&gt;

&lt;p&gt;Sanic有一个机制来保存URL和函数（一般称之为视图函数）之间的映射关系，就像&lt;code&gt;dict&lt;/code&gt;中&lt;code&gt;key&lt;/code&gt;和&lt;code&gt;value&lt;/code&gt;，这样当服务端接收到请求&lt;code&gt;http://0.0.0.0:8000/&lt;/code&gt;，就会立马知道，接下来需要调用&lt;code&gt;index()&lt;/code&gt;函数了，我们将其称之为路由。&lt;/p&gt;

&lt;p&gt;Sanic中可以用&lt;code&gt;app.route&lt;/code&gt;修饰器来定义路由，当Sanic服务启动的时候，&lt;code&gt;app.route&lt;/code&gt;就会将其中传入的参数与装饰的函数自动注册好，比如下面这段代码：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;@app.route(&amp;quot;/&amp;quot;)
async def index(request):
    &amp;quot;&amp;quot;&amp;quot;当服务端接收到客户端的/请求时，就会调用此函数&amp;quot;&amp;quot;&amp;quot;
    return text(&#39;Hello World!&#39;)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;此时请求&lt;code&gt;http://0.0.0.0:8000/&lt;/code&gt;就会返回&lt;code&gt;Hello World!&lt;/code&gt;，很显然，这不是我们想要的需求，我们的需求是展示10条资讯新闻，数据怎么来？你只需要调用&lt;code&gt;get_news()&lt;/code&gt;函数，就会获取到你想要的资讯数据：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;@app.route(&amp;quot;/&amp;quot;)
async def index(request):
    # html页面模板
    html_tem = &amp;quot;&amp;quot;&amp;quot;
    &amp;lt;div style=&amp;quot;width: 80%; margin-left: 10%&amp;quot;&amp;gt;
        &amp;lt;p&amp;gt;&amp;lt;a href=&amp;quot;{href}&amp;quot; target=&amp;quot;_blank&amp;quot;&amp;gt;{title}&amp;lt;/a&amp;gt;&amp;lt;/p&amp;gt;
        &amp;lt;p&amp;gt;{summary}&amp;lt;/p&amp;gt;
         &amp;lt;p&amp;gt;{updated_at}&amp;lt;/p&amp;gt;
    &amp;lt;/div&amp;gt;
    &amp;quot;&amp;quot;&amp;quot;
    html_list = []
    # 获取数据
    all_news = await get_news()
    # 生成在浏览器展示的html页面
    for each_news in all_news:
        html_list.append(html_tem.format(
            href=each_news.get(&#39;news_info&#39;, [{}])[0].get(&#39;url&#39;, &#39;#&#39;),
            title=each_news.get(&#39;title&#39;),
            summary=each_news.get(&#39;summary&#39;),
            updated_at=each_news.get(&#39;updated_at&#39;),
        ))

    return html(&#39;&amp;lt;hr&amp;gt;&#39;.join(html_list))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;运行此服务：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;python run news.py
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;此时，访问&lt;code&gt;http://0.0.0.0:8000/&lt;/code&gt;，你就会获得Sanic服务程序返回的资讯新闻，如下图，可以看到返回服务端提供的最新资讯：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://www.howie6879.cn/images/post/28/01_0.jpg&#34; alt=&#34;01_0&#34; /&gt;&lt;/p&gt;

&lt;p&gt;页面成功地呈现出我们想要的结果，实在是令人兴奋，等等，不能高兴太早，我们还有一个需求，要根据浏览器输入的页数来展示内容，如：&lt;code&gt;http://0.0.0.0:8000/2&lt;/code&gt;，思考一下，应该怎样优雅地完成这个需求，或许你会想，再构建一对URL与视图函数的映射关系，像下面这样：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;@app.route(&amp;quot;/2&amp;quot;)
async def page_2(request):
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;不得不说，这是一个糟糕的解决方案，这样没法解决接下来的第3页、第4页、甚至第n页（虽然目前这个服务程序只展示到第2页），最佳实践应该是把页数当做变量来获取，Sanic的路由机制自然提供了获取动态请求参数的功能，如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;@app.route(&amp;quot;/&amp;lt;page:int&amp;gt;&amp;quot;)
@app.route(&amp;quot;/&amp;quot;)
async def index(request, page=1):
    &amp;quot;&amp;quot;&amp;quot;
    支持/请求与/page请求方式
    具体的代码逻辑也会有一点改变，可参考：examples/demo01/news.py
    &amp;quot;&amp;quot;&amp;quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;再次运行此服务：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;python run news.py
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;不论是请求&lt;code&gt;http://0.0.0.0:8000/&lt;/code&gt;或者&lt;code&gt;http://0.0.0.0:8000/2&lt;/code&gt;，都是我们想要的结果。&lt;/p&gt;

&lt;h4 id=&#34;请求数据&#34;&gt;请求数据&lt;/h4&gt;

&lt;p&gt;细心的你可能会发现，每次编写一个视图函数的时候，总是有一个&lt;code&gt;request&lt;/code&gt;参数：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;async def index(request, page=1):
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;为什么必须定义这个参数，它从哪来？它有什么作用，下面我将一一为你解答。&lt;/p&gt;

&lt;p&gt;如果你在客户端请求&lt;code&gt;http://0.0.0.0:8000/&lt;/code&gt;的时候，顺手在视图函数里面打印下参数&lt;code&gt;request&lt;/code&gt;，会有如下输出：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;&amp;lt;Request: GET /&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;看终端的输出可以了解到&lt;code&gt;request&lt;/code&gt;参数实际上是一个名为&lt;code&gt;Request&lt;/code&gt;的实例对象，每当服务端接收到一个请求，Sanic的&lt;code&gt;handle_request&lt;/code&gt;函数必定会接收一个&lt;code&gt;Request&lt;/code&gt;实例对象，这个实例对象包含了一系列请求信息。&lt;/p&gt;

&lt;p&gt;前面说到，每个URL对应一个视图函数，而Sanic的&lt;code&gt;handle_request&lt;/code&gt;接下来会将接收的&lt;code&gt;Request&lt;/code&gt;实例对象作为参数传给URL对应的视图函数，也就是上面&lt;code&gt;index&lt;/code&gt;的&lt;code&gt;request&lt;/code&gt;参数，这样一来，就必须定义&lt;code&gt;request&lt;/code&gt;来接收&lt;code&gt;Request&lt;/code&gt;实例对象，其中包含的一些请求信息对视图函数来说非常重要，目前&lt;code&gt;Request&lt;/code&gt;对象提供了以下属性：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;json&lt;/li&gt;
&lt;li&gt;token&lt;/li&gt;
&lt;li&gt;form&lt;/li&gt;
&lt;li&gt;files&lt;/li&gt;
&lt;li&gt;args&lt;/li&gt;
&lt;li&gt;raw_args&lt;/li&gt;
&lt;li&gt;cookies&lt;/li&gt;
&lt;li&gt;ip&lt;/li&gt;
&lt;li&gt;port&lt;/li&gt;
&lt;li&gt;socket&lt;/li&gt;
&lt;li&gt;remote_addr&lt;/li&gt;
&lt;li&gt;path&lt;/li&gt;
&lt;li&gt;url&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;上面只是列出了一部分属性，如果你想了解更多，可查看&lt;a href=&#34;https://github.com/channelcat/sanic/blob/master/sanic/request.py&#34;&gt;request.py&lt;/a&gt;源码文件了解。&lt;/p&gt;

&lt;p&gt;为了可以实际使用下&lt;code&gt;request&lt;/code&gt;，我们可以再加一个需求，比如增加一个&lt;code&gt;GET&lt;/code&gt;请求的接口&lt;code&gt;http://0.0.0.0:8000/json&lt;/code&gt;，如果请求不设置参数&lt;code&gt;nums&lt;/code&gt;的值，则默认返回一条资讯新闻，如果设置了&lt;code&gt;nums&lt;/code&gt;参数，则该接口返回的新闻数量由参数值决定，参数最大值为10：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-json&#34;&gt;@app.route(&#39;/json&#39;)
async def index_json(request):
    &amp;quot;&amp;quot;&amp;quot;
    默认返回一条资讯，最多十条
    &amp;quot;&amp;quot;&amp;quot;
    nums = request.args.get(&#39;nums&#39;, 1)
    # 获取数据
    all_news = await get_news()
    try:
        return json(random.sample(all_news, int(nums)))
    except ValueError:
        return json(all_news)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;运行此服务：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;python run news.py
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;此时视图函数&lt;code&gt;index_json&lt;/code&gt;就可以根据接受的参数&lt;code&gt;nums&lt;/code&gt;来返回对应数量的新闻，访问&lt;code&gt;http://0.0.0.0:8000/json?nums=2&lt;/code&gt;，效果如下：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://www.howie6879.cn/images/post/28/01_1.jpg&#34; alt=&#34;01_1&#34; /&gt;&lt;/p&gt;

&lt;h4 id=&#34;响应&#34;&gt;响应&lt;/h4&gt;

&lt;p&gt;不论哪个Web框架，都是需要构建响应对象的，Sanic自然也不例外，它用的是&lt;code&gt;sanic.response&lt;/code&gt;来构建响应对象，像上面的代码中可以看到：&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;from sanic.response import html, json
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;这表示我们目前构建的资讯阅读服务，分别返回了&lt;code&gt;body&lt;/code&gt;格式为&lt;code&gt;html&lt;/code&gt;以及&lt;code&gt;json&lt;/code&gt;的响应对象，除了这两种格式，Sanic还提供了下面几种格式：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;json&lt;/li&gt;
&lt;li&gt;text&lt;/li&gt;
&lt;li&gt;raw&lt;/li&gt;
&lt;li&gt;html&lt;/li&gt;
&lt;li&gt;file&lt;/li&gt;
&lt;li&gt;file_stream&lt;/li&gt;
&lt;li&gt;stream&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;更多属性请看&lt;a href=&#34;https://github.com/channelcat/sanic/blob/master/sanic/response.py&#34;&gt;response.py&lt;/a&gt;，我们可以根据实际需求来构建响应对象，最后再返回给客户端。&lt;/p&gt;

&lt;h4 id=&#34;继续深入&#34;&gt;继续深入&lt;/h4&gt;

&lt;p&gt;不要以为现在编写的资讯服务已经很完善了，其实还有许多问题需要我们解决，比如访问&lt;code&gt;http://0.0.0.0:8000/html&lt;/code&gt;这个URL会返回：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;Error: Requested URL /html not found
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;服务程序为什么会抛出这个错误？因为程序中并路由没有注册&lt;code&gt;html&lt;/code&gt;，并且没有进行错误捕捉（比如此时的404），解决这个问题也很方便，比如把这个错误全部跳转到首页，代码如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;@app.exception(NotFound)
def ignore_404s(request, exception):
    return redirect(&#39;/&#39;)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;此时访问一些没有注册于路由的URL，比如此时的&lt;code&gt;http://0.0.0.0:8000/html&lt;/code&gt;都会自动跳转到&lt;code&gt;http://0.0.0.0:8000/&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;现在，我们已经用Sanic编写了一个简单的资讯阅读服务，在编写的过程中使用了路由、数据请求、处理以及响应对象，这些基础知识足够你编写一些基本的服务，但这还远远不够，比如模板引、引入静态文件等，这些都等着我们在实践中继续深入了解。&lt;/p&gt;

&lt;h3 id=&#34;总结&#34;&gt;总结&lt;/h3&gt;

&lt;p&gt;本章介绍了Sanic的安装以及基本的使用，目标是希望诸位可以迅速的了解并掌握Sanic的基本使用方法，并为阅读接下来的章节打一下基础。&lt;/p&gt;

&lt;p&gt;文档以及代码：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Sanic&lt;/code&gt; github地址：&lt;a href=&#34;https://github.com/channelcat/sanic&#34;&gt;https://github.com/channelcat/sanic&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;官方教程：&lt;a href=&#34;http://sanic.readthedocs.io/en/latest/&#34;&gt;http://sanic.readthedocs.io/en/latest/&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;demo地址：&lt;a href=&#34;https://github.com/howie6879/Sanic-For-Pythoner/blob/master/examples/demo01/run.py&#34;&gt;demo01&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
    </item>
    
    <item>
      <title>2.Docker - 实例演示 - owllook</title>
      <link>https://www.howie6879.cn/post/27/</link>
      <pubDate>Tue, 22 Aug 2017 20:18:06 +0000</pubDate>
      
      <guid>https://www.howie6879.cn/post/27/</guid>
      <description>

&lt;p&gt;上一篇笔记&lt;a href=&#34;http://blog.howie6879.cn/2017/08/15/26/&#34;&gt;1.Docker - 初使用&lt;/a&gt; 主要介绍了&lt;code&gt;Docker&lt;/code&gt;的安装以及一个简单的运行例子，本次笔记主要通过具体的实例来介绍一些&lt;code&gt;Docker&lt;/code&gt;镜像以及容器的基本操作&lt;/p&gt;

&lt;h3 id=&#34;1-目标&#34;&gt;1.目标&lt;/h3&gt;

&lt;p&gt;之前的毕设&lt;a href=&#34;https://github.com/howie6879/owllook&#34;&gt;owllook&lt;/a&gt;是用&lt;code&gt;python&lt;/code&gt;编写的，我将它开源在&lt;code&gt;github&lt;/code&gt;上，正借此机会，将其制作成&lt;code&gt;Docker&lt;/code&gt;镜像，以便部署&lt;/p&gt;

&lt;p&gt;本次笔记就以此项目为中心，目标是将该项目制作成&lt;code&gt;Docker&lt;/code&gt;镜像，并从过程中一步步熟悉&lt;code&gt;Docker&lt;/code&gt;&lt;/p&gt;

&lt;h3 id=&#34;2-定制镜像&#34;&gt;2.定制镜像&lt;/h3&gt;

&lt;p&gt;上一篇笔记中说了，镜像是由一系列指令一步一步构建出来，但是，最初的镜像我们还是需要从镜像仓库获取，比如&lt;code&gt;owllook&lt;/code&gt;基于&lt;code&gt;python3.6&lt;/code&gt;，那么我第一步便是从镜像仓库获取&lt;code&gt;python&lt;/code&gt;镜像&lt;/p&gt;

&lt;!-- more --&gt;

&lt;p&gt;运行命令：&lt;code&gt;docker pull python:3.6&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;稍等片刻，就会拉取一个&lt;code&gt;python3.6&lt;/code&gt;的镜像下来，让我们以这个镜像为基础来启动一个容器：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;# 具体可参考 dcker run --help 来了解详细命令
docker run -it --rm python:3.6 python

# 终端会有如下输出 此时进入了容器中的3.6环境
Python 3.6.2 (default, Jul 24 2017, 19:47:39) 
[GCC 4.9.2] on linux
Type &amp;quot;help&amp;quot;, &amp;quot;copyright&amp;quot;, &amp;quot;credits&amp;quot; or &amp;quot;license&amp;quot; for more information.
&amp;gt;&amp;gt;&amp;gt; 

#　也可以直接进入容器
docker run -it --rm python:3.6 
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;有了基础镜像，下面我们有两种方案构建&lt;code&gt;owllook&lt;/code&gt;镜像：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;使用&lt;code&gt;docker commit&lt;/code&gt; 命令&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Dockerfile&lt;/code&gt;(使用此方案)&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Dockerfile内容如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-dockerfile&#34;&gt;# 基于python3.6镜像来构建owllook镜像
FROM python:3.6
MAINTAINER howie6879 &amp;lt;howie6879@gmail.com&amp;gt;
RUN apt update -y &amp;amp;&amp;amp; apt-get install -y net-tools
# 设置环境变量
ENV APP_ROOT /opt
WORKDIR ${APP_ROOT}/
COPY requirements.txt ${APP_ROOT}/
# 安装依赖
RUN pip install --no-cache-dir --trusted-host mirrors.aliyun.com -i http://mirrors.aliyun.com/pypi/simple/ -r requirements.txt
COPY . ${APP_ROOT}
WORKDIR ${APP_ROOT}/owllook/
RUN find . -name &amp;quot;*.pyc&amp;quot; -delete
# 启动
CMD [&amp;quot;gunicorn&amp;quot;,&amp;quot;-c&amp;quot;,&amp;quot;config/dev_gunicorn.py&amp;quot;,&amp;quot;--worker-class&amp;quot;,&amp;quot;sanic.worker.GunicornWorker&amp;quot;,&amp;quot;server:app&amp;quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;构建镜像：&lt;/p&gt;

&lt;p&gt;&lt;code&gt;docker build -t owllook:0.1 .&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;构建成功后，可直接运行，不过针对&lt;code&gt;owllook&lt;/code&gt;这个项目，我设置了几个环境变量，可运行如下命令运行：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;# 运行并将docker的8001端口映射到主机的8001
# 在dev_owllook.env里面填上数据库配置 数据库ip需要注意 不得填localhost
docker run --env-file ./dev_owllook.env -d -p 8001:8001 owllook:0.1
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;仅仅是达到使用&lt;code&gt;Docker&lt;/code&gt;的程度，所花费时间真的不多，所以说&lt;code&gt;Docker&lt;/code&gt;还是很简单方便的，下一步就是上传到镜像仓库。&lt;/p&gt;

&lt;h3 id=&#34;3-上传镜像&#34;&gt;3.上传镜像&lt;/h3&gt;

&lt;p&gt;首先请注册&lt;a href=&#34;https://hub.docker.com/&#34;&gt;&lt;em&gt;Docker Hub&lt;/em&gt;&lt;/a&gt;账号，然后创建自己的仓库之后，比如这里创建好&lt;code&gt;howie6879/owllook&lt;/code&gt;，随即在终端输入：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;# 重新打包下 或者改下tag也没问题
docker build -t howie6879/owllook:0.1 .
# 根据提示登录
docker login
docker push howie6879/owllook:0.1
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;稍等即大功告成&lt;/p&gt;

&lt;h3 id=&#34;4-最后&#34;&gt;4.最后&lt;/h3&gt;

&lt;p&gt;现在可以根据&lt;code&gt;Dockerfile&lt;/code&gt;自己构建&lt;code&gt;owllook&lt;/code&gt;镜像或者直接&lt;code&gt;docker pull owllook&lt;/code&gt;镜像，确实十分方便&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>1.Docker - 初使用</title>
      <link>https://www.howie6879.cn/post/26/</link>
      <pubDate>Tue, 15 Aug 2017 20:13:30 +0000</pubDate>
      
      <guid>https://www.howie6879.cn/post/26/</guid>
      <description>

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-08-15-docker.png-blog.howie&#34; alt=&#34;docker&#34; /&gt;&lt;/p&gt;

&lt;p&gt;自己很少使用&lt;code&gt;Docker&lt;/code&gt;，也没有机会使用，毕业开始工作后，进的公司使用&lt;code&gt;Docker&lt;/code&gt;进行部署，这是我的一些笔记，以便翻阅。&lt;/p&gt;

&lt;h3 id=&#34;1-准备&#34;&gt;1.准备&lt;/h3&gt;

&lt;blockquote&gt;
&lt;p&gt;系统：ubuntu16.04 or centos &amp;hellip;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;h4 id=&#34;1-1-安装&#34;&gt;1.1.安装&lt;/h4&gt;

&lt;h5 id=&#34;1-1-1-ubuntu16-04&#34;&gt;1.1.1.ubuntu16.04&lt;/h5&gt;

&lt;p&gt;首先安装依赖包，在终端输入：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;sudo apt-get update
sudo apt-get install apt-transport-https ca-certificates
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;安装&lt;code&gt;Docker&lt;/code&gt;，自动安装脚本提供了非常简洁的安装方式：&lt;/p&gt;

&lt;!-- more --&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;# 官方下载方式
curl -sSL https://get.docker.com/ | sh

# 镜像站会提升下载速度
# 阿里云的安装脚本
curl -sSL http://acs-public-mirror.oss-cn-hangzhou.aliyuncs.com/docker-engine/internet | sh -

# DaoCloud 的安装脚本
curl -sSL https://get.daocloud.io/docker | sh
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;安装完成后，输入&lt;code&gt;sudo service docker status&lt;/code&gt; 可见&lt;code&gt;Docker&lt;/code&gt;已经启动:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;● docker.service - Docker Application Container Engine
   Loaded: loaded (/lib/systemd/system/docker.service; enabled; vendor preset: enabled)
   Active: active (running) since 二 2017-08-15 15:34:46 CST; 26min ago
     Docs: https://docs.docker.com
 Main PID: 16890 (dockerd)
    Tasks: 25
   Memory: 60.1M
      CPU: 1.980s
   CGroup: /system.slice/docker.service
&lt;/code&gt;&lt;/pre&gt;

&lt;h5 id=&#34;1-1-2-centos7&#34;&gt;1.1.2.centos7&lt;/h5&gt;

&lt;p&gt;完全可以参照上面的自动安装&lt;/p&gt;

&lt;h4 id=&#34;1-2-镜像加速器&#34;&gt;1.2.镜像加速器&lt;/h4&gt;

&lt;p&gt;配置镜像加速器的目的是为了在使用&lt;code&gt;Docker&lt;/code&gt;的过程中能提升速度，比如使用&lt;a href=&#34;https://www.daocloud.io/mirror#accelerator-doc&#34;&gt;DaoCloud 加速器&lt;/a&gt;，输入网址，点击立即使用，按照官网的教程，一行命令搞定:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;curl -sSL https://get.daocloud.io/daotools/set_mirror.sh | sh -s http://{修改分配给你的id}.m.daocloud.io
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;2-使用&#34;&gt;2.使用&lt;/h3&gt;

&lt;p&gt;下面将简单的获取一个镜像并输出一个&lt;code&gt;Hello World&lt;/code&gt;来简单演示下&lt;code&gt;Docker&lt;/code&gt;的使用，首先介绍下&lt;code&gt;Docker&lt;/code&gt;的三个基本概念，掌握了这三个基本概念，也就理解了&lt;code&gt;Docker&lt;/code&gt;的生命周期&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;什么是镜像(Image)？&lt;/strong&gt;&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;引用《第一本Docker书》&lt;/p&gt;

&lt;p&gt;镜像是构建&lt;code&gt;Docker&lt;/code&gt;世界的基石，用户基于镜像来运行自己的容器，镜像是基于联合（Union）文件系统的一种层式，由一系列指令一步一步构建出来&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;&lt;strong&gt;什么是容器(Container)？&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;em&gt;容器基于镜像启动，里面可以运行一个或者多个进程&lt;/em&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;什么是仓库(Repository)？&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;em&gt;想象下&lt;code&gt;github&lt;/code&gt;就很容易理解，托管你自己创建的镜像，默认使用&lt;a href=&#34;https://hub.docker.com/&#34;&gt;Docker Hub&lt;/a&gt;&lt;/em&gt;&lt;/p&gt;

&lt;h4 id=&#34;2-1-hello-world&#34;&gt;2.1.Hello World&lt;/h4&gt;

&lt;p&gt;运行一条命令 &lt;code&gt;docker run hello-world&lt;/code&gt;，会发现并没有运行成功，因为我是在非&lt;code&gt;root&lt;/code&gt;的情况下运行的，会报如下错误：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;docker: Got permission denied while trying to connect to the Docker daemon socket at unix:///var/run/docker.sock: Post http://%2Fvar%2Frun%2Fdocker.sock/v1.30/containers/create: dial unix /var/run/docker.sock: connect: permission denied.
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;至于为什么有这个错误，具体可以看&lt;a href=&#34;https://yeasy.gitbooks.io/docker_practice/content/install/ubuntu.html&#34;&gt;这里&lt;/a&gt;，介绍得非常清楚，下面说下解决办法：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;# 一定要重新登录才会生效
sudo usermod -aG docker $USER
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;用户重新登陆过后，就能成功运行了!&lt;/p&gt;

&lt;h4 id=&#34;2-2-简单例子&#34;&gt;2.2.简单例子&lt;/h4&gt;

&lt;p&gt;&lt;strong&gt;获取镜像&lt;/strong&gt;:&lt;/p&gt;

&lt;p&gt;&lt;code&gt;docker pull howie6879/cn_ubuntu&lt;/code&gt;，这条命令就是从&lt;code&gt;Docker Hub&lt;/code&gt;拉取一个镜像到本地，下面就可以使用这个镜像了,&lt;strong&gt;运行&lt;/strong&gt;:&lt;/p&gt;

&lt;p&gt;&lt;code&gt;docker run howie6879/cn_ubuntu:latest /bin/echo &amp;quot;Hello World&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;终端就会输出&lt;code&gt;Hello World&lt;/code&gt;&lt;/p&gt;

&lt;h3 id=&#34;3-最后&#34;&gt;3.最后&lt;/h3&gt;

&lt;blockquote&gt;
&lt;p&gt;参考1：&lt;a href=&#34;https://yeasy.gitbooks.io/docker_practice/content/index.html&#34;&gt;《Docker-从入门到实践》&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;参考2：《第一本Docker书》&lt;/p&gt;

&lt;p&gt;gist看&lt;a href=&#34;https://gist.github.com/83685e04e191079c56d36900e0839a3d&#34;&gt;这里&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;图片来自网络&lt;/p&gt;
&lt;/blockquote&gt;
</description>
    </item>
    
    <item>
      <title>gRPC使用初试</title>
      <link>https://www.howie6879.cn/post/25/</link>
      <pubDate>Thu, 03 Aug 2017 20:57:28 +0000</pubDate>
      
      <guid>https://www.howie6879.cn/post/25/</guid>
      <description>

&lt;h2 id=&#34;1-前言&#34;&gt;1.前言&lt;/h2&gt;

&lt;p&gt;&lt;a href=&#34;https://grpc.io/&#34;&gt;gRPC&lt;/a&gt;是一个开源的高性能并且能在任何环境中运行的RPC框架，其采用&lt;code&gt;protocol buffer&lt;/code&gt;:&lt;/p&gt;

&lt;p&gt;&lt;code&gt;protocol buffer&lt;/code&gt;是一个用于结构化数据序列化的一个灵活的、有效率的自动化机制，类似于XML(但比其更简单、小巧且简单)，对于某个服务需要定义的数据结构，可以使用&lt;code&gt;protocol buffer&lt;/code&gt;(&lt;code&gt;proto3&lt;/code&gt;)来进行定义，即根据&lt;code&gt;protocol buffer language&lt;/code&gt;来定义你的&lt;code&gt;protocol buffer data&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;最后再利用&lt;code&gt;protocol buffer&lt;/code&gt;编译定义的 &lt;code&gt;.proto&lt;/code&gt; 文件生成客户端和服务端代码，本文将举一个小例子进行介绍，且默认站在python的角度来编写代码&lt;/p&gt;

&lt;!-- more --&gt;

&lt;h2 id=&#34;２-实践&#34;&gt;２.实践&lt;/h2&gt;

&lt;p&gt;假设有这样一个需求，将客户端传来的某个字符串转化为小写，那么，首先从定义数据结构开始，暂时先新建如下结构的项目：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;demo
└── proto
    └── lower.proto
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;接下来将编写&lt;code&gt;proto&lt;/code&gt;文件，本例使用&lt;code&gt;proto3&lt;/code&gt;语法进行编写，&lt;code&gt;protobuf&lt;/code&gt;定义的语法请看&lt;a href=&#34;https://developers.google.com/protocol-buffers/docs/proto3&#34;&gt;这里&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;lower.proto&lt;/code&gt;文件内容如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-proto&#34;&gt;syntax = &amp;quot;proto3&amp;quot;;
package lower;

service Lower {
    rpc Lower (LowerRequest) returns (LowerResult) {
    }
}

message LowerRequest {
    string message = 1;
}

message LowerResult {
    string lower_message = 1;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;定义一个服务&lt;code&gt;Lower&lt;/code&gt;，其接受一个字符串消息并返回小写的字符串消息，编写完毕，下面进行编译，创建文件&lt;code&gt;proto_compile.py&lt;/code&gt;，并安装相关的包：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;pip install grpcio
pip install grpcio-tools
&lt;/code&gt;&lt;/pre&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# script proto_compile.py
from grpc_tools import protoc

# 或者在项目根目录运行：
# python -m grpc_tools.protoc -I. --python_out=../ --grpc_python_out=../ ./cal.proto
protoc.main(
    (
        &#39;&#39;,
        &#39;-Iproto&#39;,
        &#39;--python_out=.&#39;,
        &#39;--grpc_python_out=.&#39;,
        &#39;proto/lower.proto&#39;,
    )
)

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;运行后，项目结构如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;demo
├── lower_pb2_grpc.py     # 编译生成的文件
├── lower_pb2.py          # 编译生成的文件
├── proto
│   └── lower.proto
└── proto_compile.py

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;最后编写客户端以及服务端脚本&lt;code&gt;lower_client.py&lt;/code&gt;、&lt;code&gt;lower_server.py&lt;/code&gt;，内容分别如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# lower_server.py

import time
import grpc
from concurrent import futures
import lower_pb2, lower_pb2_grpc

_TIME_WAIT = 10


class Lower(lower_pb2_grpc.LowerServicer):
    def Lower(self, request, context):
        return lower_pb2.LowerResult(lower_message=request.message.lower())


def serve():
    server = grpc.server(futures.ThreadPoolExecutor(max_workers=10))
    lower_pb2_grpc.add_LowerServicer_to_server(Lower(), server)
    server.add_insecure_port(&#39;[::]:50052&#39;)
    server.start()
    try:
        while True:
            time.sleep(_TIME_WAIT)
    except KeyboardInterrupt:
        server.stop(0)


if __name__ == &#39;__main__&#39;:
    serve()

# lower_client.py

#!/usr/bin/env python
# -*- coding: utf-8 -*-
import grpc

import lower_pb2, lower_pb2_grpc


def run():
    channel = grpc.insecure_channel(&amp;quot;localhost:50052&amp;quot;)
    stub = lower_pb2_grpc.LowerStub(channel)
    response = stub.Lower(lower_pb2.LowerRequest(message=&amp;quot;HELLO WORLD!&amp;quot;))
    print(&amp;quot;Message result received:  %s&amp;quot; % response.lower_message)


if __name__ == &#39;__main__&#39;:
    run()

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;此时文件目录结构如下：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;demo
├── lower_client.py
├── lower_pb2_grpc.py
├── lower_pb2.py
├── lower_server.py
├── proto
│   └── lower.proto
├── proto_compile.py
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;先运行&lt;code&gt;lower_server.py&lt;/code&gt;，再运行&lt;code&gt;lower_client.py&lt;/code&gt;，就会看到终端有结果输出：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;Message result received:  hello world!
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;可以看到，返回小写字符，&lt;code&gt;demo&lt;/code&gt;演示完毕&lt;/p&gt;

&lt;h2 id=&#34;3-最后&#34;&gt;3.最后&lt;/h2&gt;

&lt;p&gt;以下是我了解&lt;code&gt;gRPC&lt;/code&gt;过程中收集的一些资料：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;p&gt;官方中文文档：&lt;a href=&#34;http://doc.oschina.net/grpc?t=58008&#34;&gt;http://doc.oschina.net/grpc?t=58008&lt;/a&gt;&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;proto中文翻译： &lt;a href=&#34;http://colobu.com/2017/03/16/Protobuf3-language-guide/&#34;&gt;http://colobu.com/2017/03/16/Protobuf3-language-guide/&lt;/a&gt;&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;参考实例：&lt;a href=&#34;http://blog.csdn.net/lavorange/article/details/74504837&#34;&gt;http://blog.csdn.net/lavorange/article/details/74504837&lt;/a&gt;&lt;/p&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
    </item>
    
    <item>
      <title>talospider - 简单的爬虫框架</title>
      <link>https://www.howie6879.cn/post/24/</link>
      <pubDate>Wed, 07 Jun 2017 23:56:08 +0000</pubDate>
      
      <guid>https://www.howie6879.cn/post/24/</guid>
      <description>&lt;h3 id=&#34;为什么写这个&#34;&gt;为什么写这个？&lt;/h3&gt;

&lt;p&gt;一些简单的页面，无需用比较大的框架来进行爬取，自己纯手写又比较麻烦&lt;/p&gt;

&lt;p&gt;因此针对这个需求写了&lt;a href=&#34;https://github.com/howie6879/talospider&#34;&gt;talospider&lt;/a&gt;:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;1.针对单页面的item提取 - 具体介绍点&lt;a href=&#34;https://github.com/howie6879/talospider/blob/master/docs/item.md&#34;&gt;这里&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;2.spider模块 - 具体介绍点&lt;a href=&#34;https://github.com/howie6879/talospider/blob/master/docs/spider.md&#34;&gt;这里&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&#34;介绍-使用&#34;&gt;介绍&amp;amp;&amp;amp;使用&lt;/h3&gt;

&lt;h4 id=&#34;item&#34;&gt;item&lt;/h4&gt;

&lt;p&gt;这个模块是可以独立使用的，对于一些请求比较简单的网站（比如只需要&lt;code&gt;get&lt;/code&gt;请求），单单只用这个模块就可以快速地编写出你想要的爬虫，比如(以下使用python3，python2见examples目录)：&lt;/p&gt;

&lt;p&gt;&lt;/p&gt;

&lt;h5 id=&#34;单页面单目标&#34;&gt;单页面单目标&lt;/h5&gt;

&lt;p&gt;比如要获取这个网址&lt;a href=&#34;http://book.qidian.com/info/1004608738&#34;&gt;http://book.qidian.com/info/1004608738&lt;/a&gt; 的书籍信息，封面等信息，可直接这样写：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;import time
from talospider import Item, TextField, AttrField
from pprint import pprint

class TestSpider(Item):
    title = TextField(css_select=&#39;.book-info&amp;gt;h1&amp;gt;em&#39;)
    author = TextField(css_select=&#39;a.writer&#39;)
    cover = AttrField(css_select=&#39;a#bookImg&amp;gt;img&#39;, attr=&#39;src&#39;)

    def tal_title(self, title):
        return title

    def tal_cover(self, cover):
        return &#39;http:&#39; + cover

if __name__ == &#39;__main__&#39;:
    item_data = TestSpider.get_item(url=&#39;http://book.qidian.com/info/1004608738&#39;)
    pprint(item_data)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;具体见&lt;a href=&#34;https://github.com/howie6879/talospider/blob/master/examples/qidian_details_by_item.py&#34;&gt;qidian_details_by_item.py&lt;/a&gt;&lt;/p&gt;

&lt;h5 id=&#34;单页面多目标&#34;&gt;单页面多目标&lt;/h5&gt;

&lt;p&gt;比如获取&lt;a href=&#34;[https://movie.douban.com/top250](https://movie.douban.com/top250)&#34;&gt;豆瓣250电影&lt;/a&gt;首页展示的25部电影，这一个页面有25个目标，可直接这样写：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;from talospider import Item, TextField, AttrField
from pprint import pprint

# 定义继承自item的爬虫类
class DoubanSpider(Item):
    target_item = TextField(css_select=&#39;div.item&#39;)
    title = TextField(css_select=&#39;span.title&#39;)
    cover = AttrField(css_select=&#39;div.pic&amp;gt;a&amp;gt;img&#39;, attr=&#39;src&#39;)
    abstract = TextField(css_select=&#39;span.inq&#39;)

    def tal_title(self, title):
        if isinstance(title, str):
            return title
        else:
            return &#39;&#39;.join([i.text.strip().replace(&#39;\xa0&#39;, &#39;&#39;) for i in title])

if __name__ == &#39;__main__&#39;:
    items_data = DoubanSpider.get_items(url=&#39;https://movie.douban.com/top250&#39;)
    result = []
    for item in items_data:
        result.append({
            &#39;title&#39;: item.title,
            &#39;cover&#39;: item.cover,
            &#39;abstract&#39;: item.abstract,
        })
    pprint(result)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;具体见&lt;a href=&#34;https://github.com/howie6879/talospider/blob/master/examples/douban_page_by_item.py&#34;&gt;douban_page_by_item.py&lt;/a&gt;&lt;/p&gt;

&lt;h4 id=&#34;spider&#34;&gt;spider&lt;/h4&gt;

&lt;p&gt;当需要爬取有层次的页面时，比如爬取豆瓣250全部电影，这时候&lt;code&gt;spider&lt;/code&gt;部分就派上了用场：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-python&#34;&gt;# !/usr/bin/env python
from talospider import Spider, Item, TextField, AttrField, Request
from talospider.utils import get_random_user_agent


# 定义继承自item的爬虫类
class DoubanItem(Item):
    target_item = TextField(css_select=&#39;div.item&#39;)
    title = TextField(css_select=&#39;span.title&#39;)
    cover = AttrField(css_select=&#39;div.pic&amp;gt;a&amp;gt;img&#39;, attr=&#39;src&#39;)
    abstract = TextField(css_select=&#39;span.inq&#39;)

    def tal_title(self, title):
        if isinstance(title, str):
            return title
        else:
            return &#39;&#39;.join([i.text.strip().replace(&#39;\xa0&#39;, &#39;&#39;) for i in title])


class DoubanSpider(Spider):
    # 定义起始url，必须
    start_urls = [&#39;https://movie.douban.com/top250&#39;]
    # requests配置
    request_config = {
        &#39;RETRIES&#39;: 3,
        &#39;DELAY&#39;: 0,
        &#39;TIMEOUT&#39;: 20
    }
	# 解析函数 必须有
    def parse(self, res):
        # 将html转化为etree
        etree = self.e_html(res.html)
        # 提取目标值生成新的url
        pages = [i.get(&#39;href&#39;) for i in etree.cssselect(&#39;.paginator&amp;gt;a&#39;)]
        pages.insert(0, &#39;?start=0&amp;amp;filter=&#39;)
        headers = {
            &amp;quot;User-Agent&amp;quot;: get_random_user_agent()
        }
        for page in pages:
            url = self.start_urls[0] + page
            yield Request(url, request_config=self.request_config, headers=headers, callback=self.parse_item)

    def parse_item(self, html):
        items_data = DoubanItem.get_items(html=html)
        # result = []
        for item in items_data:
            # result.append({
            #     &#39;title&#39;: item.title,
            #     &#39;cover&#39;: item.cover,
            #     &#39;abstract&#39;: item.abstract,
            # })
            # 保存
            with open(&#39;douban250.txt&#39;, &#39;a+&#39;) as f:
                f.writelines(item.title + &#39;\n&#39;)


if __name__ == &#39;__main__&#39;:
    DoubanSpider.start()
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;控制台：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;/Users/howie/anaconda3/envs/work3/bin/python /Users/howie/Documents/programming/python/git/talospider/examples/douban_page_by_spider.py
2017-06-07 23:17:30,346 - talospider - INFO: talospider started
2017-06-07 23:17:30,693 - talospider_requests - INFO: GET a url: https://movie.douban.com/top250
2017-06-07 23:17:31,074 - talospider_requests - INFO: GET a url: https://movie.douban.com/top250?start=25&amp;amp;filter=
2017-06-07 23:17:31,416 - talospider_requests - INFO: GET a url: https://movie.douban.com/top250?start=50&amp;amp;filter=
2017-06-07 23:17:31,853 - talospider_requests - INFO: GET a url: https://movie.douban.com/top250?start=75&amp;amp;filter=
2017-06-07 23:17:32,523 - talospider_requests - INFO: GET a url: https://movie.douban.com/top250?start=100&amp;amp;filter=
2017-06-07 23:17:33,032 - talospider_requests - INFO: GET a url: https://movie.douban.com/top250?start=125&amp;amp;filter=
2017-06-07 23:17:33,537 - talospider_requests - INFO: GET a url: https://movie.douban.com/top250?start=150&amp;amp;filter=
2017-06-07 23:17:33,990 - talospider_requests - INFO: GET a url: https://movie.douban.com/top250?start=175&amp;amp;filter=
2017-06-07 23:17:34,406 - talospider_requests - INFO: GET a url: https://movie.douban.com/top250?start=200&amp;amp;filter=
2017-06-07 23:17:34,787 - talospider_requests - INFO: GET a url: https://movie.douban.com/top250?start=225&amp;amp;filter=
2017-06-07 23:17:34,809 - talospider - INFO: Time usage：0:00:04.462108

Process finished with exit code 0
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;此时当前目录会生成&lt;code&gt;douban250.txt&lt;/code&gt;，具体见&lt;a href=&#34;https://github.com/howie6879/talospider/blob/master/examples/douban_page_by_spider.py&#34;&gt;douban_page_by_spider.py&lt;/a&gt;。&lt;/p&gt;

&lt;h3 id=&#34;说明&#34;&gt;说明&lt;/h3&gt;

&lt;p&gt;学习之作，待完善的地方还有很多，欢迎提意见，项目地址&lt;a href=&#34;https://github.com/howie6879/talospider&#34;&gt;talospider&lt;/a&gt;。&lt;/p&gt;</description>
    </item>
    
    <item>
      <title>你的浏览器可好|Chrome插件篇</title>
      <link>https://www.howie6879.cn/post/23/</link>
      <pubDate>Thu, 11 May 2017 23:47:40 +0000</pubDate>
      
      <guid>https://www.howie6879.cn/post/23/</guid>
      <description>&lt;p&gt;当我大学拥有第一部电脑开始，之后不论电脑是什么系统，我的首选浏览器都是&lt;code&gt;chrome&lt;/code&gt;，不仅仅是因为其速度快，更多是它丰富的扩展在吸引我。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;Chrome改变了我的浏览方式&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;&lt;/p&gt;

&lt;h2 id=&#34;instagram&#34;&gt;instagram&lt;/h2&gt;

&lt;p&gt;基本且常用的插件且先不说（像Adblock Plus、Advertising Terminator之类去广告的）。&lt;/p&gt;

&lt;p&gt;考虑到公众号ins的用户颇多，首先推荐这方面的插件。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;Notifications for Instagram&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;打开谷歌商店，下载&lt;code&gt;Notifications for Instagram&lt;/code&gt;这个插件的作用很简单，你不必打开ins官网就可以简单且快速的知道账户的活动信息。&lt;/p&gt;

&lt;p&gt;安装好，点击右侧工具栏，现在不论在那个网址都可以看到ins账户的实时信息了：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1101.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;h2 id=&#34;听&#34;&gt;听&lt;/h2&gt;

&lt;p&gt;qq、网易云、虾米在音乐这一领域的版权上可谓三分天下，而&lt;code&gt;listen1&lt;/code&gt;这个开源插件解决了这个问题，以后听歌不用一个一个播放器跳了。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;listen1
one for all free music in china&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;首先进入&lt;a href=&#34;http://listen1.github.io/listen1/&#34;&gt;http://listen1.github.io/listen1/&lt;/a&gt; 下载chrome插件或者应用，安装第三方来源的插件，需要启动开发者模式进行安装，如下图：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1103.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;进入插件页面，勾选开发者模式，点击从本地加载插件，选择下载的文件（解压后是文件夹）&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1104.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1105.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;qq、网易云、虾米，他们终于在一起了&amp;hellip;&lt;/p&gt;

&lt;p&gt;生的孩子叫&lt;code&gt;listen1&lt;/code&gt;，路人望天感动哭。&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;歌听完了，我还想下载怎么解&lt;/strong&gt;，别怕，还有&lt;code&gt;Sound Pirate&lt;/code&gt;&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;Sound Pirate
宣言：一切音频我都能直接下载&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;这个插件用来下载歌曲可真是6得飞起，使用很简单，安装好，当听歌的时候它的下载标识就会默默地出现。&lt;/p&gt;

&lt;p&gt;我悄悄地来，悄悄地走~&lt;/p&gt;

&lt;h2 id=&#34;阅读&#34;&gt;阅读&lt;/h2&gt;

&lt;p&gt;阅读这里面门道就多了，小说是阅读、漫画也是、博客亦然，之所以说这三个，因为这是我比较中意的需求~&lt;/p&gt;

&lt;p&gt;小说这个且先放在后面再讲，诸位可别忘了。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;漫画：ComicReader&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;这个插件集中了市面上的漫画资源，并进行解析，直接在谷歌商店安装好之后，比如说我想看海贼王。&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1106.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;一排结果出来了：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1107.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;阅读效果：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1108.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;还会提示更新：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1109.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;这款插件我用了快两年，很不错，良心插件。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;博客文章：Save to Pocket&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;这个插件可以将你在互联网上喜欢的一篇文章放进你的&lt;code&gt;pocket&lt;/code&gt;，比如说我在简书的一篇文章：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1110.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;然后不论何时何地，哪怕挤公交上厕所，你可以掏出手机，在app上或者网页上访问你&lt;code&gt;口袋&lt;/code&gt;里的知识。&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1111.png-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;h2 id=&#34;翻译&#34;&gt;翻译&lt;/h2&gt;

&lt;blockquote&gt;
&lt;p&gt;划词翻译&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;浏览网页时候，英文单词不认识怎么破，这时候就是划词翻译（开源软件）登场的时候了。&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1112.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;把不认识的单词从左滑到右，一切尽在不言中。&lt;/p&gt;

&lt;h2 id=&#34;看&#34;&gt;看&lt;/h2&gt;

&lt;p&gt;看视频？去广告？&lt;/p&gt;

&lt;p&gt;去广告&lt;/p&gt;

&lt;p&gt;刚在阅读那块写到小说放到后面讲，就是现在了，那为什么小说和视频放在一块呢，因为这里要讲一个很强大的插件Violentmonkey（油猴子）&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;Violentmonkey
作用：管理js脚本&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;这个插件可以说是万能，它可以针对性的对特定网站加载特定脚本，这个玩法太多了，值得去摸索。&lt;/p&gt;

&lt;p&gt;我这里说两个：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;1.看视频&lt;/li&gt;
&lt;li&gt;2.看小说&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;安装好&lt;code&gt;Violentmonkey&lt;/code&gt;之后，比如说在aiqiyi看视频，啊广告好烦。&lt;/p&gt;

&lt;p&gt;来这个网站&lt;a href=&#34;https://greasyfork.org/zh-CN&#34;&gt;https://greasyfork.org/zh-CN&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;检索针对aiqiyi的js脚本：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1113.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;就这个了，点击进去，安装着个脚本：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1114.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;再看视频，神奇的事情发生了，其他网站以此类推。&lt;/p&gt;

&lt;p&gt;对于小说，也是直接在这个网站找js脚本，直接安装：&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1115.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;http://oe7yjec8x.bkt.clouddn.com/howie/2017-05-11-1116.jpeg-blog.howie&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;看这阅读效果。&lt;/p&gt;

&lt;p&gt;安装了&lt;code&gt;Violentmonkey&lt;/code&gt;，以后你的浏览器使用体验，只和你的想象力有关系了，只要你能想象到，就会有脚本帮你实现。&lt;/p&gt;

&lt;p&gt;今天&lt;code&gt;Chrome&lt;/code&gt;插件推荐就这么多了，要是你有插件推荐，咋们后台说。&lt;/p&gt;</description>
    </item>
    
  </channel>
</rss>